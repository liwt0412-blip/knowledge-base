---
tags: [中州养老, 项目, 参考]
date: 2026-06-10
---

微信小程序登录
          流程图

                                                                                                1.获取用户信息                            2.获取授权码code
1. 小程序端，调用wx.getInfoProfile()获取用户信息，如用户头像、昵称
                                                                        用户授权                    wx.getInfoProfil
                                                    小程序（前端）                                                                           wx.login()
2. 小程序端，调用wx.login()获取code，就是授权码。                                                                     e()


3. 小程序端，调用wx.request()发送请求并携带code，请求开发者服务器(自己编                                                                 3.wx.request(),携带授权码code+用户信息
写的后端服务)。
                                                                                                             5.根据openid检查            6.自定义登录状态，生
4. 服务端，通过HttpClient向微信接口服务发送请求，并携带appId+appsecret
                                                                               4.用授权码换取                      用户是否存在，如                成token，返回前端一
+code三个参数。                                                                                                                           系列数据
                                                                                  openid                       不存在则新增

5. 服务端，根据openid校验用户是否已经存在，不存在新增                                   httpClient
                                                    后端微信登录接口                   jscode2session                      检验用户               生成JWT token
6. 服务端，自定义登录态，生成JWT令牌(token)

7. 将openid、token等数据返回给小程序端，方便后续请求身份校验。
                                                                        7.返给前端JWT token、openid、用户id
8. 小程序端，收到自定义登录态，存储storage。

9. 小程序端，后绪通过wx.request()发起业务请求时，携带token。
                                                                   9、携带token访问
                                                    小程序（前端）                                后端其他接口                     10.解析登录用户id                  11.返回前端业务数据
10. 开发者服务端，收到请求后，通过携带的token，解析当前登录用户的id。

11. 开发者服务端，身份校验通过后，继续相关的业务逻辑处理，最终返回业务数据
                                                      8.将token、
                                                    openid存储在本地

## 相关笔记

- [[📖 中州养老课程文档/中州养老课程总览|中州养老课程总览]]


- [[MOC-项目实战]]
