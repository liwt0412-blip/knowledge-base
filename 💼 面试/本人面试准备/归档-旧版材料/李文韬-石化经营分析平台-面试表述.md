# 李文韬-项目表述（石化经营分析平台）

## 一、自我介绍

---

面试官好，我叫李文韬，来自湖南长沙。之前一直在做 Java 后端开发，最近参与的是某能源央企省公司的经营分析平台项目。

技术栈以 Spring Boot + Spring Cloud Alibaba 微服务体系为主，中间件用过 Nacos、Gateway、RabbitMQ、Redis、XXL-Job。数据库 MySQL + MyBatis-Plus。AI 方向做过 RAG 架构全链路落地，用 Spring AI 做 Function Calling 和模型管理，Python 侧用 LangChain + Milvus + BGE-M3 做文档检索和混合搜索。

下面我详细介绍一下这个经营分析平台项目。

## 二、项目介绍

---

我参与的项目叫**经营分析平台**，是某能源央企省公司为了提升加油站和便利店经营管理效率而搭建的一套后台管理系统。

**项目背景：** 该省公司下辖 14 个地市、近 2000 座加油站，日常经营涉及设备管理、油品零售、非油商品进销存、安全巡检、客户核算等多个环节。此前各油站数据主要靠纸质表单和 Excel 填报，数据汇总滞后、无法实时监控设备状态、人工咨询成本高。平台 2023 年 2 月正式上线，此后经历了两年多的持续迭代优化——从初期的核心业务模块稳定运行，到中期扩展报表和数据分析能力，再到最近一年引入 AI 智能助手降低培训和咨询成本。

> **AI 模块推动主体：** 集团层面由总部销售事业部 2024 年初出台 AI 落地专项方案，甲方指定的技术供应商统一输出 RAG 知识库、会话管理、向量检索等标准化 Java 微服务底座。省公司层面由公司党委会正式立项审批，零售管理部提业务需求（梳理四大类知识库、70% 流量服务经营分析场景），信息管理部统筹技术落地（对接集团 AI 底座、打通经营分析平台数仓和 IoT 设备主表、完成本地 DeepSeek 大模型私有化部署）。各市分公司仅前端使用，无独立 AI 算力。

**我的参与：** 平台上线初期即加入项目，全程参与了核心业务模块的稳定运行、报表扩展和 AI 智能助手模块的本地化落地，至今约两年半。所在实施团队驻场省中心机房，与甲方信息部在同一办公环境协作——同一套代码仓库、同一套集团基线、同一个 Nacos 集群，日常工作内容和甲方开发人员没有区别。项目交付完成后进入运维期，实施团队合约到期。考虑到传统企业的技术栈迭代偏保守，个人也想往 AI 应用方向深入发展，就没有续约。

**架构形态：** 平台采用 Spring Cloud Alibaba 微服务架构，拆分为 7 个独立微服务——设备管理、账册表单、客户核算、AI 智能助手、清洁打卡、网关鉴权、消息通知。服务间通过 Nacos 注册发现、OpenFeign 同步调用、RabbitMQ 异步通信，Gateway 统一入口。

**技术栈：** 核心框架 Spring Boot + Spring Cloud Alibaba + MyBatis-Plus，缓存 Redis，消息队列 RabbitMQ，定时任务 XXL-Job。AI 模块采用 Java（Spring AI）+ Python（LangChain + Milvus + BGE-M3）双语言微服务协作架构。

**项目团队：** 后端三人——我主导 AI 智能助手模块全链路与设备 IoT 数据接入模块，其余两位分别负责账册表单+客户核算、清洁打卡+网关+通知。前端两人。各地市公司有信息人员负责系统落地和员工培训，省公司运维团队管线上环境。

**我的职责：** 基于集团 Spring Cloud Alibaba 微服务基线，主导 AI 智能助手模块的全链路设计与实现（文档加载→向量化→双路混合检索→流式生成），同时负责设备管理模块的 IoT 数据接入与状态监控开发。

## 三、业务链路与技术挑战

---

| 链路 | 核心挑战 |
|-|-|
| 设备 IoT 接入 | 约 8000 台加油机 MQTT 上报 → RabbitMQ 削峰 → Redis TTL 心跳超时检测 → XXL-Job 离线告警 |
| 账册表单填报 | 零售/非油/HSE/数质量/人事/财务六类数据 → 油站→片区→地市→省逐级汇总，数据一致性 |
| 非油调拨盘点 | 跨油站库存变更 @Transactional 原子性 + version 乐观锁防并发超扣 + Redis 分布式锁盘点隔离 |
| 客户核算分析 | 日近 20 万笔交易，T+1 预聚合到分析表（customer_id+month 联合索引），详情页当日实时 tab 补时效 |
| AI 智能问答 | Function Calling 自路由（LLM 判断调函数还是走 RAG）+ Redis MD5 缓存拦截半数以上 + Python 双路混合检索降级 |
| 清洁打卡 | 排班→扫码→记录查询，常规 CRUD |
| 网关鉴权 + 通知 | Gateway 统一入口 + JWT 鉴权 + RabbitMQ 异步推送告警/任务提醒 |

## 四、项目用到的技术

---

这个项目主要用到的技术栈有：

1. **核心框架：** Spring Boot + Spring Cloud Alibaba（微服务架构）+ Spring MVC
2. **服务注册与配置：** Nacos（注册中心 + 配置中心，全省省中心集群）
3. **网关：** Spring Cloud Gateway
4. **熔断限流：** Sentinel（流量控制 + 熔断降级）
5. **分布式事务：** Seata（跨服务事务协调）
6. **缓存服务：** Redis（缓存油站基础数据、客户档案热点数据、设备状态）
7. **消息队列：** RabbitMQ（异步处理设备上报数据、订单状态变更通知）
8. **持久层：** MyBatis-Plus（操作 MySQL 数据库）
9. **分析引擎：** ClickHouse（千万级日加油详单实时聚合查询，经营大屏秒级响应）
10. **定时任务：** XXL-Job（处理设备离线检测、报表定时生成、数据同步）
11. **AI 门面服务（Java）：** Spring AI（ChatClient 多模型调用、Function Calling、VectorStore 抽象）
12. **AI 推理服务（Python）：** FastAPI + LangChain + Milvus + BGE-M3（RAG 全流程：文档加载/切分/嵌入/混合检索/重排序）
13. **工具：** Hutool（常用工具类）、Jackson（JSON 解析）、Lombok（简化代码）、JWT（令牌认证）

这个项目主要由以下几个微服务组成：

1. **油站管理服务：** 管理加油站、加油机、加油枪等基础设备档案
2. **账册服务：** 处理零售、非油、HSE、数质量、人事、财务等各类经营数据的填报、汇总与查询
3. **核算服务：** 管理客户档案、潜在客户、客户分析清单、营销活动
4. **AI助手服务：** 提供业务操作顾问、系统运维顾问、智能答题等功能
5. **清洁打卡服务：** 处理清洁任务排班、扫码打卡、记录查询
6. **网关服务：** 统一入口，路由转发，鉴权过滤
7. **通知服务：** 处理设备告警、任务提醒等消息推送

## 五、我的职责

---

在这个项目中我主要负责了两个模块：AI 智能助手模块（全链路设计与实现）和设备管理模块（IoT 数据接入与状态监控）。平台采用集团统一技术基线（Spring Cloud Alibaba 2.2.7、Nacos 1.4.3、RabbitMQ 4.9.5、Seata 1.4.2、Sentinel 1.8.4 等），各省分公司不允许自定义框架版本。

### 模块一：设备管理服务——加油机物联网设备接入

---

#### 0、整体架构

设备管理模块核心是一条**数据上报→异步处理→状态监控**链路。真实数据流经过三层：加油机 MQTT → 油站管控机 → 市公司前置机房（仅转发+断网 48h 缓存）→ 省中心数据库——平台从省中心订阅消费数据（省侧经 RabbitMQ → Flink 实时清洗后分写入 MySQL 交易库和 ClickHouse 分析库），经 RabbitMQ 削峰填谷后异步写入 MySQL，同时 Redis 维护设备心跳状态，XXL-Job 定时扫描离线设备触发告警。平台直连省中心 MySQL 和 ClickHouse 读写业务数据——实施团队不另起数据库实例，由省中心 DBA 统一运维。

```
加油机/MQTT → 油站管控机 → 市公司前置机房(仅转发) → 省中心数据库
                                  ↓
                          平台订阅消费(HTTP)
                                  ↓
                            RabbitMQ ─┬→ 写入 device_data (MySQL)
                                      └→ Redis 心跳刷新 (TTL 5min)

XXL-Job (每5分钟) → 扫描 Redis 心跳超时 → 生成告警 → RabbitMQ → 通知服务
```

**技术要点：** RabbitMQ + Redis 组合——消息队列缓冲早晚加油高峰的数据洪峰，Redis TTL 机制天然适合心跳超时判断（设备在线就续期，离线就自动过期），比定时查 MySQL 高效得多。

---

#### 1、业务介绍

设备管理服务负责全省加油站的加油机、加油枪设备档案管理以及物联网数据接入。加油站是石油公司最核心的资产，设备运行状态直接影响经营数据采集的准确性和及时性。

核心功能包括：

- **设备档案管理：** 按"地市→片区→油站→加油机→加油枪"五级维度维护设备信息
- **设备数据采集：** 通过消息队列接收加油机上报的实时数据（加油量、液位仪数据、设备状态等）
- **设备状态监控：** 实时检测设备在线/离线状态，异常时触发告警通知
- **设备校验管理：** 记录加油机定期校验结果，管理铅封、防水滤芯更换等

#### 2、表设计

| 表名 | 说明 |
|-|-|
| **oil_station** | 油站表（油站编码、名称、地市、片区、地址等） |
| **fuel_machine** | 加油机表（关联油站ID、机器编号、型号、状态） |
| **fuel_gun** | 加油枪表（关联加油机ID、枪号、油品类型、状态） |
| **device_data** | 设备上报数据表（油站ID、设备ID、数据类型、数值、上报时间） |
| **device_alert** | 设备告警记录表（油站ID、设备ID、告警类型、告警内容、处理状态） |
| **calibration_record** | 加油机校验记录表（设备ID、校验时间、误差值、校验人） |

> device_data 表覆盖全省约 8.3 万个 IoT 终端，包括加油机 8100+、加油枪 16400+、液位仪 10000±、充电枪 4582、IoT 网关 1935、可燃气体探测器 + 视频监控等。静态维表，按月同步，不存时序心跳数据。

#### 3、接口设计

| 接口名称 | 新增加油枪 |
|-|-|
| 请求地址 | `/api/device/gun/add` |
| 请求方法 | POST |
| 请求参数 | `{ "stationCode": "430101", "machineNo": "01", "gunNo": "01", "fuelType": "92#汽油", "status": 1 }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "gunId": 1001 } }` |

| 接口名称 | 查询油站设备列表 |
|-|-|
| 请求地址 | `/api/device/station/list` |
| 请求方法 | GET |
| 请求参数 | `?cityId=4301&page=1&size=10` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "total": 50, "records": [{ "stationCode": "430101", "stationName": "中心加油站", "machineCount": 4, "gunCount": 16, "status": 1 }] } }` |

| 接口名称 | 查询设备实时状态 |
|-|-|
| 请求地址 | `/api/device/status/realtime` |
| 请求方法 | GET |
| 请求参数 | `?stationCode=430101` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "onlineCount": 4, "offlineCount": 0, "alertCount": 1, "machines": [{ "machineNo": "01", "status": 1, "lastReportTime": "2026-06-17 10:00:00" }] } }` |

| 接口名称 | 处理设备告警 |
|-|-|
| 请求地址 | `/api/device/alert/process` |
| 请求方法 | POST |
| 请求参数 | `{ "alertId": 2001, "status": 1, "processResult": "已安排维修人员现场检修" }` |
| 响应示例 | `{ "code": 200, "msg": "success" }` |

#### 4、技术实现

1. **设备数据对接与消息处理：**加油机通过MQTT协议将实时数据上报到平台的数据接收层。数据接收层对外暴露HTTP接口`/api/device/data/receive`接收JSON格式的加油量、液位仪、设备心跳等数据。接收到数据后，通过RabbitMQ进行异步分发——一条消息写入设备数据表用于历史查询和报表统计，另一条消息进入实时处理队列用于设备状态判断和告警检测。使用消息队列的好处是削峰填谷：全省约 8000 台加油机在早晚高峰集中上报（高峰期 500-1000 条/s）时，通过队列缓冲保证系统不会被打垮。
2. **设备状态判断与告警：**设备管理服务维护一个Redis缓存（key: `device:heartbeat:{deviceId}`, TTL: 5分钟），每次收到设备上报数据时更新该缓存。同时通过XXL-Job定时任务每隔5分钟扫描所有设备，检查哪些设备最近5分钟内没有上报心跳，标记为离线并生成告警记录。告警记录通过RabbitMQ发送给通知服务，通知服务通过系统消息推送给对应油站负责人。Redis的TTL机制天然适合做设备超时判断——设备只要持续上报，TTL不断续期；一旦中断，TTL到期后Redis自动过期，定时任务扫描时就能检测到，比遍历数据库判断时间差效率高得多。
3. **统一返回格式与异常处理：**前后端所有接口统一使用R对象返回格式。通过全局异常处理器`@ControllerAdvice`统一拦截业务异常和系统异常，避免每个Controller重复写try-catch。前端拿到统一的响应结构后，可以在Axios拦截器中统一处理错误提示和登录超时跳转，开发效率提升不少。
4. **MyBatis-Plus数据操作：**CRUD操作全部基于MyBatis-Plus的BaseMapper和IService，分页使用MyBatis-Plus自带的分页插件。公共字段（createTime、updateTime、createUser、updateUser）通过MyBatis-Plus的自动填充功能处理，业务代码里不需要手动赋值。

#### 5、为什么使用这个技术

- **RabbitMQ + Redis组合：** 加油站设备上报数据量大且有明显的波峰波谷特征（早晚加油高峰），用消息队列缓冲是标准做法。Redis做设备状态判断则是因为TTL机制天然适合"心跳超时"场景，比数据库轮询更高效。
- **MyBatis-Plus：** 油站管理涉及大量CRUD操作（新增油站、编辑加油机、查询设备列表等），MyBatis-Plus的代码生成器和BaseMapper能减少大量重复代码，核心业务用XML写复杂SQL，简单CRUD用注解搞定。

#### 6、存在什么问题

- **设备数据延迟：** 加油机通过MQTT上报到平台再经RabbitMQ流转，对于需要实时告警的场景（如加油机异常停机）可能延迟几十秒。
- **网络不稳定：** 部分偏远站点网络条件差，偶尔出现长时间断连，导致上报数据丢失。
- **数据一致性：** 加油机的累计加油量和系统统计量偶尔不一致，需要定时对账校准。

#### 7、如何解决

- **设备告警双通道：** 对于紧急告警，除了消息队列异步处理外，接入省中心 IoT 平台已有的实时告警推送能力，确保关键告警不丢失。
- **断线重连补偿：** 恢复网络后油站网关按时间序批量补发离线期间缓存数据，省中心 Flink 做幂等去重，不会重复统计销量。
- **日终对账任务：** 通过XXL-Job每天凌晨跑对账任务，将系统统计的日销量与加油机上报的累计销量进行比对，差异超阈值时标记异常并通知运营人员核查。

---

### 模块二：非油管理服务——便利店商品全链路管理 *（平台公共模块，同事负责，本人了解架构）*

---

#### 0、整体架构

非油管理模块以 CRUD 为主，但有三条需要特别关注的数据一致性链路：调拨审批涉及跨油站库存变更的事务操作，盘点期间需要锁定库存防止并发写入，保质期预警通过定时任务扫描推送。

```
前端 → Gateway → 非油管理服务 → MyBatis-Plus → MySQL

调拨审批流:
  创建调拨单 → 审批通过 → @Transactional(库存A-、库存B+) → 完成

盘点流程:
  锁定库存 → 生成盘点底稿 → 提交实盘数 → 比对差异 → 审核

保质期预警:
  XXL-Job (每日凌晨) → 扫描临期商品 → 推送通知
```

**技术要点：** 调拨审批的库存变更放在 `@Transactional` 保证原子性（扣了 A 的库存就一定要加到 B），乐观锁（version 字段）防止并发调拨超扣。盘点期间通过 Redis 分布式锁防止销售出库与盘点数据冲突——不用数据库行锁（`SELECT ... FOR UPDATE`）是因为盘点锁需要持有数十分钟，长事务持有 DB 锁会耗尽连接池并引发死锁。Redis 分布式锁天然有 TTL 过期机制，即使盘点中途异常退出，锁也会自动释放。

---

#### 1、业务介绍

非油管理服务负责全省加油站便利店商品的全链路管理。加油站便利店虽然面积不大，但SKU种类多（食品、饮料、润滑油、日用品等），而且涉及多个地市多个油站之间的调拨和库存管理，业务流程复杂度不低。

核心功能包括：

- **商品信息管理：** 维护便利店商品的档案信息（名称、条码、分类、售价、保质期等）
- **商品调拨审批：** 油站之间或地市仓到油站的商品调拨申请、审批、出库、入库全流程
- **便利店商品盘点：** 油站定期盘点，系统根据盘点结果自动生成盘盈盘亏记录
- **商品损耗管理：** 记录商品报损（过期、破损等），统计分析损耗率
- **商品促销管理：** 配置促销活动（满减、折扣、赠品），跟踪促销效果
- **赠品台账：** 管理赠品的入库、领用、库存
- **卫生清洁台账：** 记录便利店卫生检查情况

#### 2、表设计

| 表名 | 说明 |
|-|-|
| **product** | 商品表（商品名称、条码、分类、售价、成本价、保质期天数） |
| **inventory** | 库存表（油站ID、商品ID、库存数量、预警阈值） |
| **transfer_order** | 调拨单表（调出油站、调入油站、调拨状态、审批人） |
| **transfer_item** | 调拨明细表（关联调拨单、商品ID、数量） |
| **check_record** | 盘点记录表（油站ID、盘点时间、盘点人） |
| **check_item** | 盘点明细表（商品ID、账面数量、实际数量、差异） |
| **waste_record** | 损耗记录表（商品ID、数量、损耗原因、处理方式） |
| **promotion** | 促销活动表（活动名称、开始时间、结束时间、类型、规则） |

#### 3、接口设计

| 接口名称 | 新增商品 |
|-|-|
| 请求地址 | `/api/nonoil/product/add` |
| 请求方法 | POST |
| 请求参数 | `{ "productName": "农夫山泉550ml", "barcode": "6901234567890", "categoryId": 101, "salePrice": 2.00, "shelfLife": 365 }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "productId": 5001 } }` |

| 接口名称 | 发起商品调拨 |
|-|-|
| 请求地址 | `/api/nonoil/transfer/create` |
| 请求方法 | POST |
| 请求参数 | `{ "fromStationCode": "430101", "toStationCode": "430102", "items": [{ "productId": 5001, "quantity": 100 }], "reason": "库存不足" }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "transferId": 3001, "status": 0 } }` |

| 接口名称 | 审批调拨单 |
|-|-|
| 请求地址 | `/api/nonoil/transfer/approve` |
| 请求方法 | POST |
| 请求参数 | `{ "transferId": 3001, "approveStatus": 1, "approveRemark": "同意调拨" }` |
| 响应示例 | `{ "code": 200, "msg": "success" }` |

| 接口名称 | 查询商品库存（分页） |
|-|-|
| 请求地址 | `/api/nonoil/inventory/list` |
| 请求方法 | GET |
| 请求参数 | `?stationCode=430101&categoryId=101&page=1&size=20` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "total": 150, "records": [{ "productId": 5001, "productName": "农夫山泉550ml", "quantity": 500, "alertThreshold": 50, "status": "normal" }] } }` |

| 接口名称 | 提交盘点结果 |
|-|-|
| 请求地址 | `/api/nonoil/check/submit` |
| 请求方法 | POST |
| 请求参数 | `{ "stationCode": "430101", "items": [{ "productId": 5001, "expectedQty": 500, "actualQty": 498, "diff": -2 }] }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "checkId": 4001, "diffCount": 2, "diffAmount": -4.00 } }` |

#### 4、技术实现

1. **商品管理CRUD：**商品管理模块是典型的增删改查，基于MyBatis-Plus的BaseMapper实现，分页查询使用MyBatis-Plus的分页插件PageHelper。商品分类使用树形结构存储（parent_id方式），前台展示时通过递归查询构建分类树。商品条码做了唯一索引，防止重复录入。
2. **调拨审批流：**调拨申请提交后状态变为"待审批"，通过Gateway网关根据当前用户的角色判断是否有审批权限。审批通过后，系统自动更新调出方库存（减库存）和调入方库存（加库存），这个操作放在同一个事务中保证数据一致性。如果审批拒绝，调拨单状态变为"已拒绝"，库存不做变动。
3. **盘点流程与差异处理：**盘点时先锁定油站库存（防止盘点期间有销售出库），生成盘点底稿。员工逐项录入实际数量后提交，系统比对账面数量和实际数量，自动计算差异。差异在合理范围内（如损耗率<0.5%）自动通过审核，超范围的需要管理员人工确认并填写原因。
4. **保质期预警：**通过XXL-Job每天凌晨扫描所有商品，查询距离保质期到期不足30天的商品，生成预警推送给对应油站负责人。同时支持按商品分类、油站维度查询临期商品清单，方便运营人员处理（降价促销或报损）。

#### 5、为什么使用这个技术

- **MyBatis-Plus：** 非油管理接口绝大部分是单表CRUD加简单关联查询，MyBatis-Plus基本不需要写SQL，开发效率高。分页查询也是框架自带的，省去自己写limit/offset的麻烦。
- **事务保证数据一致性：** 调拨审批通过后的库存变更涉及两张表（调拨单状态更新 + 双方库存更新），放在一个事务里执行，防止出现"扣了库存但没加到对方"的情况。
- **定时任务做预警：** 保质期预警这种场景不需要实时触发，每天凌晨跑一次就够了，XXL-Job支持分布式部署，比Spring自带的@Scheduled更可靠。

#### 6、存在什么问题

- **并发调拨冲突：** 同一个商品同时被多个调拨单操作时，可能出现库存超扣的情况。
- **盘点期间业务中断：** 盘点时需要锁定库存，盘点期间该油站不能有销售出库，影响正常业务。
- **临期商品处理不及时：** 预警虽然是每天推送，但油站负责人可能忽略，导致商品过期报损。

#### 7、如何解决

- **乐观锁防止超扣：** 库存更新时使用MyBatis-Plus的乐观锁机制（version字段），更新时校验version，版本不一致则更新失败，提示重试。
- **盘点窗口优化：** 将盘点时间安排在凌晨低峰期（通过XXL-Job定时盘点），避开白天营业高峰。同时支持"动盘"模式（只盘点有变动的商品，不锁定全部库存）。
- **临期商品升级提醒：** 对于距离保质期不足7天的商品，预警等级升级，通过短信或企业微信消息直接通知油站站长，不再仅仅依赖系统内消息。

---

### 模块三：客户核算服务——客户档案与经营分析 *（平台公共模块，同事负责，本人了解架构）*

---

#### 0、整体架构

客户核算模块的核心是**T+1 汇总 + 实时查询双轨**——客户消费明细数据量大（全省每天近 20 万笔交易），不能用实时聚合做报表。通过 XXL-Job 每天凌晨按集团标准 ODS→DWD→DWS→ADS 四层数仓建模规范汇总到分析表，查询直接走 ADS 应用层；详情页提供当日实时 tab 直接查交易流水补时效性。

```
前端 → Gateway → 客户核算服务 → MyBatis-Plus → MySQL

客户CRUD:
  Controller → Service → Mapper → customer / customer_contract

报表分析:
  XXL-Job (每日凌晨) → 聚合当日交易 → 写入 customer_analysis
  前端查询 → 直接查汇总表 (毫秒级)

六进活动管理:
  创建活动 → 跟踪执行 → 录入效果数据 → 计算 ROI
```

**技术要点：** 汇总表替代实时聚合是核心优化——交易流水表数据量太大，每次查报表都实时 count/sum 数据库扛不住。每日凌晨定时汇总到分析表，查询走汇总表，配合 customer_id + month 联合索引实现毫秒级响应。

---

#### 1、业务介绍

客户核算服务负责全省加油站客户信息的统一管理和经营数据分析。石化公司不仅要管"油"，还要管"客户"——特别是大客户（物流公司、车队、企业客户）的消费情况和信用额度。

核心功能包括：

- **客户档案管理：** 创建和维护客户档案（加油卡客户、小额配送客户、石化钱包用户），包括基本信息、信用额度、合同信息
- **潜在客户管理：** 记录潜在客户信息，跟踪开发进度，支持批量导入
- **客户分析清单：** 按消费频次、消费金额、活跃度等维度生成客户分析报表
- **六进活动管理：** 管理"进企业、进社区、进机关"等营销活动，记录活动效果和客户转化情况

#### 2、表设计

| 表名 | 说明 |
|-|-|
| **customer** | 客户表（客户名称、类型、联系方式、信用额度、状态） |
| **customer_contract** | 客户合同表（合同编号、客户ID、合同金额、有效期） |
| **potential_customer** | 潜在客户表（企业名称、联系人、电话、来源、开发状态） |
| **customer_analysis** | 客户分析数据表（客户ID、统计月份、消费金额、消费次数） |
| **marketing_activity** | 营销活动表（活动名称、类型、开始时间、结束时间、参与油站） |
| **activity_result** | 活动效果表（关联活动ID、新增客户数、销售额、投入成本） |

#### 3、接口设计

| 接口名称 | 创建客户档案 |
|-|-|
| 请求地址 | `/api/customer/create` |
| 请求方法 | POST |
| 请求参数 | `{ "customerName": "XX物流公司", "customerType": "fleet", "contactPerson": "张三", "contactPhone": "13800138000", "creditLimit": 500000 }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "customerId": 6001 } }` |

| 接口名称 | 查询客户分析报表 |
|-|-|
| 请求地址 | `/api/customer/analysis/list` |
| 请求方法 | GET |
| 请求参数 | `?month=2026-06&page=1&size=20&sortBy=amount&sortOrder=desc` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "total": 200, "records": [{ "customerName": "XX物流公司", "totalAmount": 150000, "totalCount": 45, "avgAmount": 3333, "activeDays": 22, "rank": 1 }] } }` |

| 接口名称 | 新增潜在客户 |
|-|-|
| 请求地址 | `/api/customer/potential/add` |
| 请求方法 | POST |
| 请求参数 | `{ "companyName": "XX运输公司", "contactPerson": "李四", "source": "六进活动-进企业", "status": "contacting" }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "potentialId": 7001 } }` |

| 接口名称 | 创建营销活动 |
|-|-|
| 请求地址 | `/api/customer/activity/create` |
| 请求方法 | POST |
| 请求参数 | `{ "activityName": "6月进企业推广活动", "activityType": "enterprise", "startTime": "2026-06-01", "endTime": "2026-06-30", "stations": ["430101", "430102"] }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "activityId": 8001 } }` |

#### 4、技术实现

1. **客户档案CRUD：**客户档案管理也是标准的增删改查，使用MyBatis-Plus实现。客户类型用字典表维护（加油卡、小额配送、石化钱包），支持模糊搜索和分页查询。客户信用额度变更时，需要记录变更日志，方便财务审计追溯。
2. **客户分析报表：**客户分析数据不是实时计算的，而是通过XXL-Job每天晚上统计当天的消费数据，按客户维度汇总后存入客户分析数据表。查询时直接从汇总表取数，避免每次查询都对大量交易记录做实时聚合。支持按消费金额、消费次数、活跃天数等维度排序和筛选。报表接口使用了MyBatis-Plus的分页查询，排序字段通过参数动态传入，避免了硬编码多个查询方法。SQL层面做了索引优化——在客户ID和统计月份上建了联合索引，查询效率基本是毫秒级。
3. **六进活动管理：**活动管理包含活动创建、活动执行跟踪和活动效果统计三个环节。活动创建时选择参与油站和活动类型，活动结束后运营人员录入效果数据（新增客户数、销售额等），系统自动计算投入产出比（ROI），为后续活动预算提供数据参考。

#### 5、为什么使用这个技术

- **汇总表替代实时聚合：** 客户消费明细数据量庞大（全省每天近 20 万笔交易），如果每次查报表都去聚合原始交易数据，数据库扛不住。通过定时任务提前汇总，查询时走汇总表，性能问题基本不存在。
- **MyBatis-Plus + 动态排序：** 客户分析报表的排序维度不固定（有时按金额、有时按频次），用MyBatis-Plus的LambdaQueryWrapper动态拼接排序条件，代码比写多个XML映射简洁得多。

#### 6、存在什么问题

- **数据时效性：** 汇总表是T+1的（隔天更新），运营人员当天下班前查到的客户数据其实是昨天的，不能实时反映当天情况。
- **客户重复问题：** 不同油站录入的客户可能重复（同一个物流公司在多个油站加油），但当时没有做客户去重。
- **活动效果难量化：** 六进活动的新增客户，很难精确归因到某个具体活动（客户可能是多渠道过来的）。

#### 7、如何解决

- **实时+定时双轨：** 在客户分析明细页增加"今日实时"tab，直接查当天的交易记录（限制只查当天，数据量小），首页报表仍用T+1汇总数据，两者互不干扰。
- **客户统一认证：** 基于客户统一社会信用代码或营业执照号做去重，同一企业主体在系统内只保留一条客户记录，各油站通过关联关系引用。
- **活动归因标记：** 客户注册时增加"来源活动"字段，新注册客户必须选择或由系统自动匹配最近参加过的活动，后续分析时按来源活动分组统计。

---

### 模块四：AI助手服务——大模型应用

---

#### 0、整体架构

AI 助手模块采用 **Java + Python 双语言微服务协作** 架构。路由由 Spring AI 的 **Function Calling 机制自驱动**——不设独立路由规则或分类模型，LLM 自己判断是否需要调业务函数、是否需要检索知识库。

```
                        ┌──────────────────────────────────┐
                        │     Spring Cloud Gateway          │
                        │     (统一入口 / 鉴权 / 路由)       │
                        └─────┬──────────────────┬─────────┘
                              │                  │
             ┌────────────────▼──┐   ┌──────────▼───────────────────┐
             │  AI 门面服务(Java) │   │  设备/账册/核算等(Java)        │
             │  Spring AI        │   │                               │
             │                   │   │                               │
             │  ┌─────────────┐  │   │                               │
             │  │ ① Redis 缓存 │  │   │                               │
             │  │ MD5(问题)    │  │   │     ┌──────────────────┐      │
             │  │ 命中→返回    │──┼───┼────→│     Redis        │      │
             │  │ (<5ms)      │  │   │     │  缓存回答+热点数据  │      │
             │  └─────┬───────┘  │   │     └────────▲─────────┘      │
             │        │未命中    │   │              │                │
             │        ▼         │   │        RAG命中→回写            │
             │  ┌────────────────┐  │              │                │
             │  │② Function      │  │              │                │
             │  │  Calling 自路由 │  │              │                │
             │  │                │  │              │                │
│  │ LLM 自己判断：  │  │              │                │
│  │ •调函数→拿数据  │  │              │                │
│  │ •不调→转发RAG  │  │              │                │
             │  └───────┬───────┘  │              │                │
             │          │降级      │              │                │
             └──────────┼─────────┘              │                │
                        │ REST API 调用          │                │
                        ▼                        │                │
        ┌────────────────────────────────────┐   │                │
        │  AI 推理服务 (Python)               │   │                │
        │  FastAPI + LangChain               │   │                │
        │                                    │   │                │
        │  ┌─ ③ RAG 检索 ────────────────┐  │   │                │
        │  │ BGE-M3 → Milvus → Reranker  │──┼───┘                │
        │  │ BM25 作为内部可选前置步骤     │  │                    │
        │  │ 生成回答 → 回写 Redis → 返回  │  │                    │
        │  └──────────────────────────────┘  │                    │
        │                                    │                    │
        │  📄 文档加载   ✂️ 递归切分          │                    │
        │  🧮 向量化     🔍 双路混合检索      │                    │
        │  🎯 重排序     📡 SSE 流式输出     │                    │
        └────────────────┬───────────────────┘                    │
                         │                                        │
                         ▼                                        │
                  ┌──────────┐                                    │
                  │  Milvus  │                                    │
                  │ 向量数据库│                                    │
                  └──────────┘                                    │
```

**分层职责：**

| 层 | 位置 | 核心技术 | 职责 |
|-|-|-|-|
| ① Redis 缓存 | Java | Spring Data Redis + MD5 | 高频问题命中直接返回，半数以上请求零网络开销 |
| ② Function Calling 自路由 | Java | Spring AI @Tool + ChatClient | **无独立路由规则**。LLM 自己决定：调函数拿业务数据 / 直接回答 / 降级转发 Python |
| ③ RAG 检索 | Python | LangChain + BGE-M3 + Milvus | LLM 不会回答时降级到此——语义检索 + LLM 生成，BM25 作为内部可选前置步骤 |

**路由机制——为什么不需要独立路由层：**

Spring AI 的 Function Calling 机制本身就是一个天然路由器。我把设备查询、客户查询、告警查询等接口注册为 `@Tool` 函数后，**LLM 自己推理**判断要不要调函数。路由规则只有两条，判断标准是 Function Calling 的 API 返回——100% 确定：

```
LLM 调了函数 → 拿到 MySQL 数据 → 基于数据生成回答 ✅（Java 侧完成）
LLM 没调函数 → REST 转发 Python 走 RAG ✅
```

```
用户: "430101 设备状态"
  → LLM 推理: "需要查设备 → 调 getDeviceStatus('430101')"
  → 拿到 MySQL 实时数据 → 回答 ✅

用户: "怎么建商品"
  → LLM 没调函数 → 转发 Python RAG → 检索文档 → 按操作手册回答 ✅

用户: "你好"
  → 白名单过滤（3行正则）→ ChatClient 直答 ✅
```

**白名单优化：** "你好""谢谢""再见""今天星期几"这类无内容消息，加 3 行正则前置过滤，直接交给 ChatClient 回复。不算路由规则，纯粹是避免让 RAG 管道处理无意义的问候。这是极简路由的 trade-off——其余所有 LLM 不调函数的问题都走 RAG，牺牲少量冗余 RAG 调用，换取零误判的确定性路由。

**不需要 BERT 分类器：** 垂类 Agent 场景下通用问题几乎不存在，一个 90% 准确率的分类模型放在 LLM 前面做路由，等于把 LLM 的能力限制在分类器的准确率之内。LLM 自己的判断力远高于 90%。

**为什么 Layer ① 放在 Java：** Spring Data Redis 本地集成，缓存命中零网络开销。放在 Python 的话 HTTP 往返会吃掉 <5ms 优势。

**为什么 Layer ③ 在 Python：** BGE-M3 嵌入、SPARSE_INVERTED_INDEX、BGE Reranker——Python 生态独有。Spring AI 的 VectorStore 只做 CRUD，没有文档处理和混合检索能力。

---

#### 1、业务介绍

AI助手服务为业务平台提供智能化能力支撑，面向三类使用场景：

- **业务操作顾问：** 油站工作人员在操作系统中遇到"如何新建便利店商品"、"怎么填报损耗数据"等问题时，AI助手基于业务知识库给出步骤指引。这对减少油站人员培训成本和降低咨询电话量很有帮助。
- **系统运维顾问：** IT运维人员遇到系统报错或配置问题时，AI助手提供故障排查建议和解决方案。
- **智能答题助手：** 省公司定期组织员工进行安全规范、业务流程等考试，AI助手可以自动根据知识库内容生成考题和答案解析，支撑培训考核。

AI助手采用**RAG（检索增强生成）架构**：将公司内部的操作手册、业务流程文档、安全规范等文档进行向量化处理存入向量数据库，用户提问时先检索相关文档片段，再结合大模型生成回答，保证回答内容严格基于公司内部资料，不产生"幻觉"。

**关键数字：** 知识库涵盖操作手册、安全规范、培训课件、故障案例四大类共一万两千多份文档。日均调用量 4 万多，月末业务高峰期可达 7.5 万。日活跃用户 1100-1300 人，Redis 缓存命中率超过一半。

#### 2、表设计

| 表名 | 说明 |
|-|-|
| **ai_qa_record** | 问答记录表（用户ID、问题、回答、文档引用列表、评分） |
| **ai_knowledge_base** | 知识库文档表（文档标题、文档内容、向量ID、分类、上传时间） |
| **ai_feedback** | 用户反馈表（记录ID、评价、改进建议） |
| **conversations** | 会话记录表（会话ID、用户ID、角色、消息内容、消息类型、时间戳） |
| **conversation_summary** | 会话摘要表（会话ID、摘要内容、关键实体、创建时间） |

#### 3、接口设计

| 接口名称 | AI助手问答 |
|-|-|
| 请求地址 | `/api/ai/assistant/chat` |
| 请求方法 | POST |
| 请求参数 | `{ "userId": 1001, "question": "如何新建便利店商品？", "scene": "business" }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "answer": "操作步骤如下：1. 进入非油管理模块…", "references": ["非油管理操作手册V2.3.pdf"], "relatedQuestions": ["如何修改商品信息？", "如何进行商品调拨？"] } }` |

| 接口名称 | 知识库文档上传 |
|-|-|
| 请求地址 | `/api/ai/knowledge/upload` |
| 请求方法 | POST |
| 请求参数 | 表单上传（文件 + 文档分类标签） |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "docId": 5001, "chunkCount": 25 } }` |

#### 3.1 知识库运维机制

知识库文档的日常更新不需要开发人员介入。运营人员通过管理后台上传新手册、替换旧版本、删除过期文档，上传后系统自动触发 Python 服务做切分→嵌入→写入 Milvus，全程自动化。

文档版本通过 `upload_time` 和 `doc_category` 字段管理，检索时优先匹配最新的文档版本。后端开发的工作集中在系统建设阶段——做好上传工具和管理后台，上线后文档维护是运营侧的日常工作。

#### 4、技术实现

**4.1 检索架构（Redis 缓存 + RAG 向量检索）**

````
检索分为两层——第一层在 Java 侧零网络开销命中，第二层在 Python 侧走语义检索。路由不设独立规则或分类模型，由 Spring AI 的 Function Calling 机制自驱动。

**第一层——Redis缓存命中（Java 侧）：**

用户提问后，先用问题的 MD5 哈希值作为 key 去 Redis 查。如果命中，直接返回缓存答案，响应时间 <5ms。这层主要拦截高频重复问题——油站员工问得最多的是"怎么登录""怎么建商品""怎么改密码"这类，缓存命中率超过一半。

Redis 未命中时，请求进入 Spring AI 的 ChatClient。注意：这里**没有 if-else 路由逻辑**——LLM 看到带 `@Tool` 注解的函数列表后，自己推理判断要不要调函数、调哪个、传什么参数。

路由规则只有两条，不靠 LLM 的文本输出判断（LLM 太自信，不会主动说不知道），而是靠 Function Calling 的 API 返回——100% 确定：

```
LLM 调了函数 → 拿到 MySQL 数据 → 基于数据生成回答 ✅（Java 侧完成，不到 Python）
LLM 没调函数 → REST 转发 Python 走 RAG ✅
```

举例：
- "430101 设备状态" → LLM 推理"需要查设备状态 → 调 getDeviceStatus('430101')" → 拿到数据 → 回答
- "怎么建商品" → LLM 没调函数 → 转发 Python RAG → 检索《非油管理操作手册》→ 基于文档回答
- "你好" → LLM 没调函数 → 转发 RAG → Milvus 无匹配 → LLM 自然回复"你好，我是小石"

**为什么这个极简路由在垂类场景下足够——不需要 BERT 分类器：**

这个 AI 助手不是通用 ChatBot，是业务平台的垂类 Agent。三个机制保证了用户只会问业务问题：

1. **场景限制：** 助手嵌在平台后台，上下文就是加油站运营——员工打开它是为了查设备、填报表、报故障，不是为了聊天。
2. **System Prompt 限制：** "你是经营分析平台的专属助手，只回答加油站运营、设备管理、安全规范、账册表单等业务问题。非业务问题统一回复：'我不处理这类问题，请咨询相关技术人员。'"
3. **用户行为可预测：** 油站站长、营业员、巡检员的工作内容高度固定。

垂类 Agent 不需要意图分类器去区分"通用 vs 业务"——因为在这个语境下，通用问题几乎不存在。路由逻辑可以极简：LLM 调了函数就用数据回答，没调函数就查知识库。这也是为什么我微调了 BERT 达到 90% 准确率但最终没有上——一个 90% 的分类器每 100 个问题会误判 10 个，把专业问题错误放行让 LLM 硬编，在业务安全场景下不可接受。垂类场景下前置分类不是优化，是增加误差链路。

**第二层——RAG向量检索+生成（Python 侧）：**

降级到 Python 的请求走 LangChain RAG 全流程：BGE-M3 嵌入 → Milvus 双路混合检索（IVF_FLAT + SPARSE_INVERTED_INDEX）→ BGE Reranker 重排序 → LLM 生成。生成后回写 Redis（TTL=24h），下次同样问题直接 Layer ① 命中。

BM25 关键词检索作为 RAG 内部的可选前置步骤——候选问答库中命中时直接返回，未命中时自动过渡到向量检索。不独立成层，避免增加架构复杂度。

这种两层架构的好处是：半数以上问题在 Redis 命中，剩余走 Function Calling 或 RAG。路由判断标准是 API 返回而非文本分析（100% 确定）。垂类场景下不需要意图分类器，LLM 自己就是最好的路由器。
````

**4.2 文档预处理——父子块分层切分策略**

```
我采用了"父块-子块"双层切分策略：

- **父块**（900字符）：按分隔符递归切分，保留完整的段落/章节结构。Markdown文档用MarkdownTextSplitter按标题层级切，普通文档用ChineseRecursiveTextSplitter按句号/换行等分隔符递归拆。
- **子块**（300字符）：在父块基础上进一步切分，保证每个片段粒度足够细，能精准匹配用户问题。

存储时，子块内容向量化存入Milvus用于精确检索；父块内容作为标量字段关联存储。检索时，用子块匹配到最相似的片段后，返回对应的父块内容作为大模型的上下文——这样既保证了检索精度（小粒度匹配），又保证了上下文完整性（大粒度喂给LLM）。
```

**4.3 双路混合检索——为什么检索管道在 Python**

```
Spring AI 也有 RAG 能力（VectorStore 抽象层），但只能做单路稠密向量相似度搜索——`similaritySearch("问题", topK)`。而生产级 RAG 需要的 BGE-M3 稠密+稀疏双向量嵌入、BGE Reranker 交叉编码器重排序——这些模型都是 PyTorch 实现，无法在 JVM 上运行。所以检索管道天然归属 Python 侧：向量由 Python 生成后写入 Milvus，Milvus 的索引和检索操作本身 Java 也能做，但喂给它的是什么向量、怎么重排序——这些前置和后置环节离了 Python 跑不了。

在我的架构里，Spring AI 的 VectorStore 负责知识库的**管理面**（管理员用的后台功能），LangChain 负责知识库的**数据面**（用户提问时的检索管道）：

| 操作 | 谁做 | 用什么 |
|------|------|--------|
| 上传文档 → 写入 Milvus | Java / Spring AI | `MilvusVectorStore.add()` |
| 删除文档 | Java / Spring AI | `MilvusVectorStore.delete()` |
| 管理员查看知识库状态 | Java / Spring AI | VectorStore 元数据查询 |
| 用户提问 → 文档切分 → 嵌入 | Python / LangChain | ChineseRecursiveTextSplitter + BGE-M3 |
| 用户提问 → 双路混合检索 | Python / LangChain + pymilvus | IVF_FLAT + SPARSE_INVERTED_INDEX + WeightedRanker |
| 用户提问 → 重排序 | Python / LangChain | BGE Reranker |
| 用户提问 → 生成回答 | Python / LangChain | LLM |

好处：管理面跟 Java 业务系统整合（权限、配置、审计日志），检索面用 Python 最成熟的 NLP 工具链。两边通过 REST API 通信，Spring AI 上传文档时调 Python 做切分+嵌入，拿到向量后由 Java 侧写入 Milvus。

**以下是 Python 侧检索管道的详细设计：**


这是整个RAG流程的核心——单一的向量检索召回效果有限，我采用了"稠密+稀疏"双路加权混合检索：

- **稠密向量（语义检索）：** 基于BGE-M3模型对用户问题编码，用IVF_FLAT倒排索引在Milvus中做ANN近似最近邻搜索（nprobe=10），找到"意思相近"的文档。
- **稀疏向量（词汇匹配）：** BGE-M3同步生成的稀疏向量（本质是词频权重向量），用SPARSE_INVERTED_INDEX索引做词汇级匹配，找到包含"相同关键词"的文档。
- **加权融合：** 稠密路径权重1.0、稀疏路径权重0.7，各取Top-K结果后通过Milvus的WeightedRanker合并排序。稠密权重大于稀疏，是因为业务场景下用户措辞多变（"建商品"可能说成"新增货物""加个商品"），语义理解比关键词匹配更重要。

两路召回后再做一次二次重排——用BGE Reranker Large交叉编码器模型，把用户问题和每个召回的父块内容拼成(query, doc)对逐一打分，按相关性分数重排后取Top-M返回。这一步相当于让一个专门的"裁判"再筛一遍，把最相关的内容排到最前面。加了Reranker后检索精度有明显提升，尤其对意图模糊的长问题。
```

**4.3.1 Milvus 字段设计与索引设计**

````
双路混合检索能跑起来，底层靠的是 Milvus 的 Collection Schema 和索引配置。下面是我设计的完整方案：

**Collection 字段设计：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | VARCHAR(64) | 主键，chunk 内容的 MD5 哈希（配合 upsert 实现幂等） |
| `doc_id` | VARCHAR(64) | 所属文档 ID，关联知识库文档表 |
| `doc_title` | VARCHAR(256) | 文档标题，检索结果展示来源 |
| `doc_category` | VARCHAR(64) | 文档分类（操作手册/安全规范/培训课件），Partition Key |
| `parent_text` | VARCHAR(2000) | 父块原文（~900 字符），检索命中后作为 LLM 上下文返回 |
| `child_text` | VARCHAR(1000) | 子块原文（~300 字符），用于向量化后的精确匹配 |
| `chunk_index` | INT64 | 块在文档中的序号，用于追溯上下文位置 |
| `dense_vector` | FLOAT_VECTOR(1024) | BGE-M3 稠密向量，1024 维 |
| `sparse_vector` | SPARSE_FLOAT_VECTOR | BGE-M3 稀疏词权向量，维度由词典动态决定 |
| `upload_time` | INT64 | 上传时间戳，用于时效性过滤 |

**索引设计：**

| 字段 | 索引类型 | 参数 | 选型理由 |
|------|---------|------|---------|
| `dense_vector` | IVF_FLAT | `nlist=128`, `metric=IP` | IVF_FLAT 将 128 个聚类中心的向量按簇分组，检索时只搜 `nprobe=10` 个最近簇（扫约 7.8% 数据），精度损失 1% 换来 10 倍加速。`IP`（内积）= 余弦相似度（BGE-M3 输出已 L2 归一化） |
| `sparse_vector` | SPARSE_INVERTED_INDEX | `drop_ratio_build=0.2` | 倒排索引天然适合稀疏向量——每个词项指向包含它的文档列表。`drop_ratio_build=0.2` 在建索引时丢弃词频最低的 20% 词项，减少噪音 |
| `id` | 主键索引 | 自动 | Milvus 自动维护 |
| `doc_id` | 标量索引 | Inverted Index | 支持按文档精准过滤：`expr="doc_id == 'xxx'"` |
| `doc_category` | 标量索引 | Inverted Index | 支持按分类过滤 + Partition Key 剪枝 |
| `upload_time` | 标量索引 | 排序索引 | 支持"检索最近 30 天内上传的文档"这类时效性过滤 |

**检索时的实际参数：**

```
稠密检索: nprobe=10, limit=20, metric_type=IP
稀疏检索: drop_ratio_search=0.2, limit=20
混合融合: WeightedRanker(dense=1.0, sparse=0.7)
最终输出: Top-10 → BGE Reranker 重排 → Top-K 返回
```

**为什么用 MD5 做主键而不是自增 ID：** 文档更新后重新切分，同一段文本的 MD5 不变，配合 upsert 直接覆盖旧向量，不会产生重复数据。自增 ID 则每次插入新行，旧数据变成"僵尸向量"占着空间污染检索结果。
````

**4.4 多格式文档OCR加载**

```
业务文档格式很杂：有Word操作手册、PDF规章制度、PPT培训课件，甚至还有纸质的巡检表扫描件。我封装了统一的文档加载器，根据不同文件后缀自动路由到对应的加载器（PDF用OCRPDFLoader、Word用OCRDOCLoader、PPT用OCRPPTLoader、图片用OCRIMGLoader），底层基于PaddleOCR做光学字符识别提取文字，确保图文混排的文档也能完整提取。
```

**4.5 三类Prompt + LLM参数设计**

````
虽然底层都是同一套 RAG 架构，但三个助手的角色完全不同。我设计了一个**共享身份底座**（保证语气一致），加上三个**场景 Prompt 层**（行为区分）。Java 侧和 Python 侧使用同一套 Prompt 模板。

**共享身份底座（三个助手共用）：**

```
你是"小石"，某能源央企省公司经营分析平台的 AI 助手。
你的回答必须严格基于提供的文档内容和系统数据，不得编造。
语气：简洁、专业、不废话。使用中文回答。
如果提供的文档不足以回答问题，回复"我目前没有找到相关信息，建议联系人工客服或查阅操作手册"。
如果用户的问题与业务无关，回复"我不处理这类问题，请咨询相关技术人员"。
```

**场景 Prompt 层（按角色叠加）：**

| 角色 | 追加指令 | 输出格式要求 |
|------|---------|------------|
| **业务操作顾问** | "你是业务操作指导。严格按照文档中的操作步骤回答，禁止自由发挥和猜测。每个步骤标注序号。如果文档没有明确描述某一步，不要自行补充。" | `第1步：... 第2步：... 引用来源：《xxx》` |
| **系统运维顾问** | "你是系统运维顾问。可以结合故障案例给出排查建议。回答时先判断问题类型（设备故障/系统报错/配置问题），再给出可能原因和解决方案。" | `【问题类型】→【可能原因】→【解决方案】→【参考资料】` |
| **智能答题助手** | "你是考试出题助手。基于提供的文档内容自动生成考题。每题包含题干、4个选项（单选）、正确答案和解析。题干不能照抄文档原文，需改写为提问形式。解析要引用文档原文作为依据。" | `题干 + A/B/C/D + 答案：X + 解析：...` |

**Java 侧 Function Calling 的 System Prompt（追加）：**

```
你可以使用以下工具函数查询系统的实时数据。当用户问到具体的设备状态、客户信息、告警记录等内容时，优先调用对应的函数获取最新数据，而不是依赖训练知识回答。如果没有合适的函数可用，告诉用户你不知道。
```

**LLM 参数设计：**

| 参数 | 业务操作顾问 | 系统运维顾问 | 智能答题助手 | 选型理由 |
|------|-----------|-----------|-----------|---------|
| `temperature` | 0.1 | 0.3 | 0.7 | 操作指引必须确定性输出（0.1），运维允许轻度变体（0.3），出题需要多样性（0.7） |
| `top_p` | 0.8 | 0.85 | 0.9 | 操作顾问收窄候选词分布，防止"创造性"措辞 |
| `max_tokens` | 1024 | 1536 | 2048 | 操作步骤简洁，运维方案中等长度，出题含题干+选项+解析最长 |
| `presence_penalty` | 0 | 0 | 0.3 | 出题需要避免重复用语，其余场景不需要 |
| `frequency_penalty` | 0 | 0 | 0.2 | 同上 |
| `stop` | 无 | 无 | `["---"]` | 出题用分隔符控制每题独立输出 |

**为什么三个角色的 temperature 差这么大：** 操作顾问的 temperature=0.1 是最关键的设计——业务操作（盘点、调拨、校验）错了会造成实际损失。低 temperature 确保同样的文档输入产生同样的输出，不会出现第一次说"第3步点保存"、第二次说"第3步点提交"这种不一致。
````

**4.6 SSE流式输出**

```
大模型生成回答通常需要 3-5 秒，如果等全部生成完再返回，用户体验很差。我实现了 SSE（Server-Sent Events）流式输出，前端每收到一个 token 就逐字渲染，让用户感知到系统"在打字"而不是"卡住了"。

技术实现上，Python 侧 FastAPI 使用 `StreamingResponse` 包装 LangChain 的 `astream()` 调用，每个 token 以 `data: {token}` 格式推送。前端用 EventSource API 接收，每收到一个 data 事件就追加到 UI。

SSE 比 WebSocket 更适合这个场景——问答是一问一答的单向流，不需要双向通信。SSE 基于标准 HTTP 长连接，实现更简单、防火墙兼容性也更好。
```

**4.7 选型决策：RAG vs 微调、SpringAI vs 直接调用**

```
- **选RAG不选微调：** 业务文档频繁更新（油价调整通知、新安全规范下发），RAG只需替换知识库文件即可生效，迭代成本为零。微调则需要重新标注数据、重新训练，每次更新成本高。而且RAG能引用文档来源："这段回答来自《非油管理操作手册V2.3》"，可信度远远高于微调模型。

- **选SpringAI三层用途（不是简单调API）：**
    1. **ChatClient 直调 LLM：** 对于不需要检索知识库的简单问题（闲聊、操作指引），Java 侧用 SpringAI 的 ChatClient 直接调 LLM。切换模型供应商（通义千问→DeepSeek）只改配置，代码零改动。
    2. **Function Calling（Agent 能力）：** 用 SpringAI 的 `@Tool` 注解把系统业务函数暴露给 LLM。比如员工问"查一下 430101 油站的实时设备状态"，LLM 自动调用 `getDeviceStatus()` 从数据库拿到真实数据再回复——这让 AI 不止能背文档，还能查真实业务数据。
    3. **VectorStore 抽象层：** 知识库管理后台（上传、删除、查状态）在 Java 侧通过 SpringAI 的 MilvusVectorStore 操作，不直接写 pymilvus 代码。好处是未来换 Qdrant 或其他向量数据库，改配置即可，业务代码不动。

- **SpringAI + LangChain 双框架分工（不是冗余，是各管对方做不好的事）：**

    Spring AI 和 LangChain 不是功能重复，而是解决两个不同层面的问题——Spring AI 负责"业务整合"，LangChain 负责"NLP 流水线"。

    **分工逻辑：**

    | 维度 | Spring AI（Java） | LangChain（Python） |
    |------|------------------|---------------------|
    | 核心职责 | AI 能力嵌入企业 Spring Cloud Alibaba 体系 | NLP 处理全流程编排 |
    | 它的主场 | Function Calling 调业务数据、认证鉴权、配置管理 | 文档加载切分、嵌入编码、混合检索 |
    | 换对方做 | LangChain 查 Spring Bean 调数据库？绕一大圈 | Java 调 BGE-M3 做嵌入？PyTorch 模型无法在 JVM 运行 |

    **三个具体场景说明为什么必须双框架：**

    1. **查业务数据（Spring AI 主场）：** 员工问"430101 油站设备状态"，LLM 需要调 `getDeviceStatus()` 从 MySQL 拿实时数据。Spring AI 的 `@Tool` 注解直接暴露 Spring Bean 方法，LLM 推理出参数 → 调函数 → 拿到结果。LangChain 要绕 HTTP 调用 Java REST 接口，多一跳且丢类型安全。

    2. **文档入知识库（LangChain 主场）：** 上传一份《安全巡检规范.pdf》，需要 PaddleOCR 提取文字 → ChineseRecursiveTextSplitter 递归切分 → jieba 分词 → BGE-M3 嵌入 → 写入 Milvus。BGE-M3 稠密+稀疏双向量嵌入和 BGE Reranker 重排序都是 PyTorch 模型，只能在 Python 侧运行。Spring AI 的 VectorStore 只能做 Milvus 的增删查管理面操作，没有嵌入生成和重排序能力。

    3. **切换模型供应商（平手）：** 通义千问 → DeepSeek，Java 改 `application.yml` 一行，Python 改环境变量一行，都是零代码切换——说明两个框架的抽象层都选对了。

    **边界通信：** 如果一个请求既需要查业务数据又要检索知识库，Java 侧先走 Function Calling 拿业务数据，再把用户问题转发到 Python 走 RAG，两边结果合并后由 Python 侧 LLM 统一生成回复。微服务架构天然支持多语言——Python 服务和 Java 服务同样注册 Nacos、走 Gateway。
```

**4.8 会话记忆与持久化存储**

````
大模型每次调用都是无状态的——不记得上一轮说了什么。AI 助手需要一套外部记忆层来维护对话的连续性。我设计了两层记忆架构：

```
① 会话记忆（Session Memory）
   ↓ 单次对话窗口内的短期上下文 → Redis
② 持久化记忆（Persistent Memory）
   ↓ 跨会话的长期存储 → MySQL + 会话摘要
```

**第一层：会话记忆——当前对话窗口的上下文连续性**

用户在一次对话中连续追问时，AI 需要知道前几轮说了什么。比如用户先问"怎么建商品"，再问"那建完之后呢"，AI 得知道"建完之后"指的是"建完商品之后"。

实现上基于 Spring AI 的 ChatMemory 接口，底层用 Redis 存储每轮对话消息。关键设计：

- **conversationId 隔离：** 每个对话窗口创建时生成唯一 ID，传递给 MessageChatMemoryAdvisor。不显式传 conversationId 时，Spring AI 默认用同一个标识——不同用户、不同会话的记忆会互相串通。解决方式是在对话入口通过 Advisor 上下文参数显式传入每个会话的唯一 conversationId。

- **最近 10 轮窗口：** 每次生成回答前取出最近 10 轮对话拼入 Prompt，旧消息自动丢弃。现在 LLM 上下文窗口都在 128K tokens 以上，10 轮对话撑死 2000 tokens，不需要做额外的摘要压缩。

- **打断输出不影响记忆保存：** 用户中途点"停止生成"时，SSE 流被中断，Spring AI 的 ChatMemory 后置增强可能不会被触发，导致已输出的内容丢失。解决方案是在流处理中用 StringBuilder 缓存已生成内容，在 doOnCancel 回调中手动调用 ChatMemory.add 写入 Redis——保证即使被打断，已吐出的内容也不丢。

**第二层：持久化记忆——跨对话窗口的长期存储**

会话记忆只在一次对话窗口内有效，用户关闭页面第二天再打开，AI 就失忆了。第二层解决跨越对话的问题。

- **存储策略——MySQL 共享表 + 会话摘要：** MySQL `conversations` 表存会话元信息（标题、时间、标签）和会话摘要。每次对话结束，LLM 自动生成 2-3 句摘要存储。不是把所有历史对话原文全量保留——那样存储和检索成本太高。

- **检索策略——按时间排序，LLM 自己判断相关性：** 用户再次提问时，取出最近 10 条会话摘要，按时间倒序注入上下文。不做向量检索——LLM 的推理能力足够判断"这条历史跟当前问题有没有关系"，不需要在外面挡一层 embedding 检索。额外引入向量检索反而增加了精度调优的负担（阈值怎么设、漏召回怎么办）。

- **跨服务共享——Java 和 Python 打通记忆：** 平台的 AI 助手有 Java 网关和 Python RAG 两个服务。如果各自单独存记忆，用户会感觉这个助手"精神分裂"。解决方案是 MySQL 的 `conversations` 表双写双读——Java 侧 Function Calling 完成后写入，Python 侧 RAG 生成完成后写入。每次生成前取出最近 5 轮对话。这保证了跨侧追问的连贯性——用户在 Java 侧调了 Function Calling 查设备状态，再在 Python 侧问"上次巡检呢"，Python 能从历史中拿到"430101"这个主语。用户感觉不到跨服务。

**为什么不做更复杂的记忆层**

一些 AI 系统会在上面两层之外再加"知识记忆层"——从对话中做实体识别、关系抽取、知识图谱存储，比如提取出"用户A 经常查询 设备430101"这样的结构化事实。我在平台没有做这一层，原因是 LLM 本身就是推理引擎——把历史摘要（纯文本）注入 Prompt，LLM 自己能判断"A 和 B 有没有关系"。额外引入 NER + 关系抽取链路，每次对话都要跑，token 开销和延迟都不小，而场景下用户行为高度可预测（查设备、建商品、填报表），不需要知识图谱来"发现"模式。如果面试官追问备用方案，我可以展开讲。

> **💡 追问备用：更复杂的记忆机制**
>
> 如果面试官问"除了这两层还有什么增强方向"，可以从以下方向展开：
>
> **③ 向量检索记忆——语义相似度召回历史**
>
> 当前方案是按时间排序取最近 10 条，适合会话量不大的场景。如果用户历史会话量很大（几百条），可以引入向量检索：把每条会话摘要用 BGE-M3 做 embedding 存入向量库，用户新问题时同样做 embedding → 向量相似度召回最相关的 3-5 条历史记忆注入上下文。这比"全量堆最近 N 条"更精准，也省 token。代价是增加向量检索链路，需要调 embedding 模型。
>
> **④ 知识记忆层——从对话中提取结构化事实**
>
> 不是存对话原文，而是从对话中抽取可复用的知识。流程：用户对话 → NER 实体识别（设备编号、客户名、操作类型）→ 关系抽取 → 三元组存储（如 `用户A — 关注 — 设备430101 — 信任度 0.9`）。下次用户登录时，AI 可以主动说"你关注的 430101 设备当前状态正常"。这比被动问答更显智能。需要额外的 NER 模型或 LLM 抽取调用，每条对话都跑一遍有延迟和 token 开销，适合用户行为复杂、需要主动服务的场景。
>
> **⑤ 记忆压缩——Token 超限的三策略**
>
> 如果 LLM 上下文窗口较小或对话特别长，可以用三级递进压缩：截断早期消息 → LLM 摘要压缩（10 轮压成 2 轮摘要，保留设备编号、操作类型等关键实体）→ 按话题分段（同一话题压成一个摘要块，切换话题时替换旧块）。优先级递进，默认只做截断，超限时才启用压缩。
>
> **⑥ 遗忘机制——时间衰减 + 信任评分**
>
> 记忆不是存越多越好。时间衰减：最近一周的记忆权重×1.0，一个月×0.7，三个月以上×0.3——越新的记忆越重要。信任评分：新信息和已有记忆矛盾时，不直接覆盖，而是降低信任度，低于阈值标记"待确认"。用户也可以明确说"你记错了"，系统定位那条记忆并修正。防止错误记忆污染，也防止过期信息干扰新对话。
````

#### 5、为什么使用这个技术

- **Redis 缓存 + Function Calling 自路由而非全部走RAG：** 油站员工问的大多是重复问题，Redis 缓存拦截半数以上请求；剩余走 Function Calling 或降级到 RAG。没有独立路由层，LLM 自己判断该走哪条路。这比"所有问题都走 RAG"的方案大幅降低了延迟和大模型调用量。
- **Function Calling 自路由而非规则路由：** 不设独立的意图分类器或正则路由。Spring AI 的 Function Calling 机制本身就是路由器——LLM 看到 `@Tool` 函数列表后自动推理判断是否需要调函数、调哪个、传什么参数。不确定时自动降级到 RAG。一个 90% 准确率的分类模型放在 LLM 前面做路由，反而把 LLM 的能力限制在分类器的准确率之内。
- **RAG而非微调：** 业务文档频繁更新（油价调整、新政策下发），RAG模式下只需更新知识库文档，不需要重新微调模型，迭代成本极低。而且RAG能明确告诉用户"这段回答来自哪份文档"，可信度更高。
- **双路混合检索而非单路：** 稠密+稀疏双路互补——稠密向量能理解"建商品"= "新增货物"（语义），稀疏向量能精准匹配"油品""损耗"等专业术语（词频），两者取长补短。加了Reranker二次重排后检索精度有明显提升。
- **SpringAI框架：** 不是简单调API——ChatClient 封装多模型切换、@Tool 注解实现 Function Calling 让 LLM 能查真实业务数据、VectorStore 抽象层统一操作向量数据库。三项能力都绑在 Spring Cloud Alibaba 体系里，与业务代码零摩擦。
- **SSE流式输出：** 2-5秒的生成等待中逐字渲染，杜绝用户"以为卡住了"的焦虑感。
- **两层记忆而非三层：** 会话记忆用 Redis + ChatMemory 保证单次对话连贯性，持久化记忆用 MySQL + 会话摘要解决跨会话连续性。没有引入向量检索记忆和知识图谱层——LLM 本身就是推理引擎，把历史摘要注入 Prompt 让 LLM 自己判断相关性就够了，额外引入 embedding 检索和实体抽取反而增加链路复杂度和 token 开销。两层足够解决实际问题，不需要为了"完整"而过度设计。

#### 6、存在什么问题

- **知识库更新延迟：** 业务文档更新后，如果管理员忘记上传新版本，AI助手给的还是旧文档的答案，可能误导员工。这是RAG架构的固有问题——知识库不会自动同步。
- **检索精度边界：** 部分操作流程比较接近（如"新建商品"和"新建促销"），偶尔检索到不相关的文档片段。虽然加了Reranker，但交叉编码器的判别能力也有上限。
- **成本控制：** 云端大模型按token计费，每次RAG调用消耗约2000-4000 token。Redis 缓存已经过滤掉半数以上，Function Calling 再拦截一部分，剩余走 RAG 的请求仍需持续关注成本。一个优化方向是对高频 RAG 问题也做 Redis 缓存（TTL=24h），同类问题直接返回缓存结果。
- **路由极简的 trade-off：** LLM 没调函数就全部转发 RAG，虽然保证了零误判的确定性路由，但有时 LLM 的训练知识完全够用（如简单问候、常识问题），仍不可避免多走一趟 RAG。当前通过 3 行白名单正则过滤无内容消息缓解，未来可考虑引入轻量意图模型做更精细的路由。

#### 7、如何解决

- **知识库版本管理：** 在文档管理后台增加版本标记和生效日期字段，AI助手优先回答生效日期内的文档。同时在管理端增加"知识库健康度"看板，监控文档更新状态和覆盖率。
- **检索优化——Hybrid Search增强：** 目前的双路混合检索权重是固定的（稠密1.0/稀疏0.7）。后续可以引入自适应权重：对关键词密集型问题自动调高稀疏权重，对描述型问题调高稠密权重，进一步提升召回精度。
- **RAG回答缓存：** 对高频RAG问题的最终回答做Redis缓存（TTL=24小时），同类问题直接返回缓存结果，进一步降低大模型调用成本。
- **BERT 意图分类技术储备：** 在探索阶段基于 BERT-base-chinese 微调了意图二分类模型，准确率达到 90% 以上。但在工程落地时发现：Spring AI 的 Function Calling 机制本身就能完成路由判断，不需要额外的分类器。一个 90% 准确率的分类器放在 LLM 前面做路由，等于把 LLM 的能力限制在分类器的准确率之内——LLM 自己的判断力远高于 90%。这个探索让我意识到：在 AI 工程中，能用系统自身能力解决的问题，不要引入额外模型增加误差链路。后续若需要更精细的多场景路由，可以在 Java 侧部署 ONNX 推理或通过 gRPC 调用 Python 模型服务。

---

## 附录：集团技术基线版本（备查）

> 以下为集团强制基线版本，各省分公司 maven 父 pom 统一管控，不允许自定义。

| 组件 | 版本 | 说明 |
|-|-|-|
| Spring Boot | 2.3.12.RELEASE | 核心微服务基线（JDK 8）。AI 门面服务由甲方指定的技术供应商统一提供标准化组件，独立部署于 JDK 17 容器，不属于省公司自定义范围 |
| Spring Cloud Alibaba | 2.2.7.RELEASE | 集团强制基线 |
| Spring Cloud Gateway | 2.2.9.RELEASE | 统一 API 网关 |
| Nacos | 1.4.3 | 全省唯一省中心集群 |
| Sentinel | 1.8.4 | 流量控制 + 熔断降级 |
| Seata | 1.4.2 | 分布式事务协调 |
| RabbitMQ | 4.9.5 | 消息队列 |
| MyBatis-Plus | 3.4.3 | ORM 持久层 |
| XXL-Job | 2.3.1 | 定时任务调度 |
| MySQL | 8.0 | 交易库（主从 + 读写分离） |
| ClickHouse | 22.8 LTS | OLAP 实时分析库（千万级日详单） |
| Redis | 6.2.7 | 分布式缓存集群 |
| Flink | 1.13.6 | 实时数据清洗（Java） |
| DataX | 3.0 | 离线数据同步 |
| OpenJDK | 8 / 17 | 核心微服务 JDK 8（信创兼容）。AI 门面服务 JDK 17（甲方指定的技术供应商标准化组件自带基线，非省公司自定义） |