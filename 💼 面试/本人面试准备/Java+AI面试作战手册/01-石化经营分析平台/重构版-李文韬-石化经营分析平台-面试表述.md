# 李文韬-石化经营分析平台-面试表述（重构版）

> 目标：用于 Java 后端 + AI 应用方向面试。  
> 核心口径：复杂企业业务系统 + Java AI 应用落地。  
> 注意：不主动说过多参数和数字，面试官追问再展开。

---

## 一、自我介绍

面试官好，我叫李文韬，来自湖南长沙。之前主要做 Java 后端开发，最近参与的是某能源央企省公司的经营分析平台项目。

这个项目技术栈以 Spring Boot + Spring Cloud Alibaba 微服务体系为主，用到了 Nacos、Gateway、RabbitMQ、Redis、XXL-Job、MyBatis-Plus、MySQL 等。项目后期引入了 AI 智能助手，我参与了 Java + AI 应用落地，主要做业务工具封装、知识库接入、RAG 检索链路联调、缓存和会话历史打通。

我的定位是 Java 后端工程师，能把大模型能力接进已有企业业务系统，关注的不只是接 API，而是权限、缓存、成本、延迟、会话和稳定性这些落地问题。

---

## 二、项目介绍

我参与的项目叫经营分析平台，是某能源央企省公司为加油站和便利店经营管理搭建的一套后台系统。业务覆盖 14 个地市、近 2000 座加油站，涉及设备管理、油品零售、非油商品进销存、安全巡检、客户核算、报表分析和 AI 智能问答。

项目 2023 年 2 月上线，后续持续迭代。早期重点是核心业务跑稳，中期扩展报表和经营分析能力，最近一年开始引入 AI 智能助手，用来降低一线员工培训和咨询成本。

### 项目架构

平台采用 Spring Cloud Alibaba 微服务架构，主要包括：

| 服务 | 说明 |
|---|---|
| 设备管理服务 | 加油站、加油机、加油枪、IoT 设备数据接入和状态监控 |
| 账册表单服务 | 零售、非油、HSE、数质量、人事、财务等经营数据填报 |
| 客户核算服务 | 客户档案、客户分析、营销活动和经营核算 |
| AI 助手服务 | 业务操作顾问、系统运维顾问、智能答题助手 |
| 清洁打卡服务 | 排班、扫码打卡、记录查询 |
| 网关鉴权服务 | Gateway 统一入口、JWT 鉴权 |
| 消息通知服务 | 告警、任务、审批通知 |

服务间通过 Nacos 做注册发现，OpenFeign 做同步调用，RabbitMQ 做异步解耦，Gateway 做统一入口。

### 我的职责

我直接负责两个方向：

1. **AI 智能助手省公司本地化落地和关键链路实现**
   包括业务工具封装、知识库接入、RAG 检索链路联调、Redis 缓存、SSE 流式输出、会话历史打通。

2. **设备 IoT 接入和状态监控**
   包括设备上报数据接入、RabbitMQ 削峰、Redis TTL 心跳状态、XXL-Job 离线告警。

非油、客户核算、网关通知等模块由团队其他成员负责，我了解整体链路和关键设计，主要用于联调和排障。

### 职责边界口径

AI 模块不是我一个人从零发明整套底座。集团侧有统一 AI 底座和规范，我负责的是省公司经营分析平台的本地化落地：把现有 Java 业务能力接入 Function Calling，把省公司知识库接入 RAG 检索链路，并补齐缓存、会话、流式输出和业务系统对接。

---

## 三、核心业务链路与技术挑战

| 链路 | 核心挑战 | 解决方案 |
|---|---|---|
| 设备 IoT 接入 | 早晚高峰集中上报，设备状态需要及时判断 | RabbitMQ 削峰，Redis TTL 维护心跳，XXL-Job 定时扫描离线 |
| 非油调拨盘点 | 调拨库存一致性、盘点期间并发写入 | `@Transactional` 保证原子性，version 乐观锁防超扣，Redis 分布式锁隔离盘点 |
| 客户核算分析 | 日交易量大，实时聚合压力高 | T+1 预聚合到 ADS 应用层，详情页查当日流水补实时性 |
| AI 智能问答 | 成本、延迟、幻觉、业务实时数据查询 | Redis 缓存 + Function Calling + RAG + 引用来源 + 兜底 |

---

## 四、AI 助手模块

### 4.1 业务场景

AI 助手面向三个场景：

1. **业务操作顾问**
   一线员工不知道系统功能怎么操作时，直接问 AI，AI 基于操作手册给步骤指引。

2. **系统运维顾问**
   运维人员输入错误信息或异常现象，AI 基于故障案例和运维文档给排查建议。

3. **智能答题助手**
   基于安全规范、培训课件生成题目、答案和解析，辅助内部培训。

### 4.2 当前生产主链路

当前主链路统一为：

```text
用户问题
  -> Java AI 助手入口
  -> Redis 缓存命中则直接返回
  -> 未命中进入 Spring AI
  -> 模型触发 Function Calling：调用 Java 业务函数查实时数据
  -> 未触发工具调用：转发 Python RAG 服务检索知识库
  -> 生成回答，SSE 流式返回
  -> 高频结果回写 Redis
```

这里没有独立的 BERT 意图分类器作为主路由。工具是否被调用、参数是什么、返回结果是什么，都能被系统结构化记录，比解析模型输出文字更可观测。

### 4.3 为什么不用 BERT 分类器做主链路

BERT 分类器在探索阶段做过，二分类准确率可以做到 90% 以上。但最终没有放入主链路，原因有三个：

1. 这是垂类业务助手，用户大多数问题本来就是业务问题，通用闲聊比例很低。
2. 前置分类器会引入额外误差，误判后可能让专业问题绕过知识库。
3. Spring AI 的 Function Calling 已经能完成业务工具调用判断，链路更短，也更容易观测。

所以当前口径是：

> BERT 是探索和技术储备，不是生产主链路。

### 4.4 Java + Python 分工

| 侧 | 负责内容 | 原因 |
|---|---|---|
| Java / Spring AI | 鉴权、业务函数、Function Calling、缓存、会话、知识库管理入口 | 与现有企业 Java 微服务体系集成最自然 |
| Python / LangChain | OCR、文档切分、Embedding、Milvus 混合检索、Reranker | BGE-M3、BGE Reranker 等模型在 Python/PyTorch 生态更成熟 |

这个分工不是重复造轮子，而是让 Java 做企业集成，Python 做 NLP 流水线。

### 4.5 RAG 检索链路

RAG 侧核心流程：

```text
文档上传
  -> OCR/文本抽取
  -> 父子块切分
  -> BGE-M3 生成稠密向量和稀疏向量
  -> 写入 Milvus

用户提问
  -> BGE-M3 编码
  -> 稠密语义检索 + 稀疏关键词检索
  -> WeightedRanker 融合
  -> BGE Reranker 重排序
  -> Top 文档片段注入 Prompt
  -> LLM 生成回答并返回引用来源
```

父子块策略：

- 子块用于精确匹配，粒度更小。
- 父块用于喂给模型，保证上下文完整。

不要主动说固定数值，除非面试官追问。可以说：

> 具体 chunk 大小是根据文档结构和离线效果调出来的，不是固定套模板。原则是检索粒度要小，生成上下文要完整。

### 4.6 幻觉控制

RAG 能降低幻觉，但不能说彻底消灭幻觉。当前做法：

1. 回答基于检索到的内部文档或 Function Calling 返回的真实业务数据。
2. 回答附带引用来源，方便用户确认依据。
3. 检索相关性不足时走兜底，不强行回答。
4. 用户反馈“有用/没用”用于后续优化检索参数和知识库质量。

标准说法：

> 我们不是让模型自由发挥，而是尽量让它基于内部资料和业务系统数据回答。幻觉不能说完全杜绝，只能通过检索质量、阈值、引用来源和兜底机制持续降低。

### 4.7 成本和延迟控制

主要靠三层：

1. **Redis 缓存**
   高频重复问题直接返回，避免每次都调模型。

2. **Function Calling**
   能查业务数据的问题直接调用 Java 函数，不走 RAG。

3. **RAG 结果缓存**
   高频 RAG 问题的最终回答设置 TTL，短期内复用。

标准说法：

> AI 成本不是理论问题，实际落地时必须控制调用次数。我们不是所有问题都走大模型，而是先缓存，再工具调用，最后才 RAG 生成。

### 4.8 会话记忆

会话分两层：

| 层 | 存储 | 作用 |
|---|---|---|
| 短期记忆 | Redis / ChatMemory | 单次会话内保留最近若干轮上下文 |
| 持久历史 | MySQL conversations 表 | Java 和 Python 服务共享历史，保证跨服务追问连贯 |

关键坑：

> Spring AI 使用 ChatMemory 时必须显式传 conversationId，否则不同用户、不同窗口的记忆可能串到一起。入口处要为每个会话生成唯一 conversationId，并在 Advisor 上下文里传递。

---

## 五、设备 IoT 模块

### 5.1 业务场景

设备管理服务负责全省加油站加油机、加油枪、液位仪、IoT 网关等设备档案和状态监控。

核心链路：

```text
加油机/MQTT
  -> 油站管控机
  -> 市公司前置机房
  -> 省中心
  -> RabbitMQ
  -> MySQL/ClickHouse/Redis
```

平台侧主要消费省中心汇聚后的数据，不单独维护底层设备传输链路。

### 5.2 技术实现

1. **RabbitMQ 削峰**
   早晚高峰设备集中上报，通过消息队列缓冲，避免接口被瞬时流量打满。

2. **Redis TTL 心跳**
   每次设备上报时刷新 `device:heartbeat:{deviceId}`，设备持续在线就续期，断开后 TTL 过期。

3. **XXL-Job 离线扫描**
   定时扫描设备状态，发现离线后生成告警，通过 RabbitMQ 推送到通知服务。

### 5.3 面试讲法

> 设备模块的核心不是 CRUD，而是数据上报、异步处理和状态判断。RabbitMQ 解决流量峰值，Redis TTL 解决心跳超时，XXL-Job 做离线告警补偿。相比轮询数据库，Redis TTL 更适合设备在线状态这种短生命周期数据。

---

## 六、常见追问标准答案

### 6.1 你 AI 模块到底做了什么？

集团侧提供统一 AI 底座和规范，我主要做省公司经营分析平台的本地化落地。具体包括三块：第一，把设备查询、告警查询、客户查询这些 Java 业务能力封装成 Spring AI 工具函数；第二，把省公司操作手册、安全规范、培训课件接入知识库，联调 Python 侧 RAG 检索链路；第三，做缓存、会话历史和流式输出这些工程化能力，保证实际使用时延迟和成本可控。

### 6.2 为什么不用 BERT 做意图分类？

BERT 分类器探索过，准确率能到 90% 以上。但这个场景是垂类业务助手，用户大多数问题都是业务问题。前面加一个 90% 的分类器，反而会把一部分专业问题误判出去。最后选择 Function Calling 做结构化工具调用，没触发工具的问题再走 RAG，链路更简单，也更可观测。

### 6.3 怎么控制 AI 成本？

第一层是 Redis 缓存，高频重复问题直接返回。第二层是 Function Calling，能查业务数据的直接查数据库，不走 RAG。第三层才是 RAG 检索和大模型生成。RAG 生成后的高频答案也会回写缓存。这样不是所有问题都调大模型，成本和延迟都能降下来。

### 6.4 怎么减少幻觉？

回答基于两类依据：一类是 Function Calling 查到的实时业务数据，一类是 RAG 检索到的内部文档。RAG 回答会附带来源，相关性不足时走兜底，不强行回答。幻觉不能说完全消灭，只能通过检索质量、引用来源、阈值和反馈机制持续降低。

### 6.5 为什么 Java + Python 双语言？

Java 侧负责企业系统集成，包括鉴权、业务函数、配置、缓存、会话、知识库管理入口。Python 侧负责 NLP 流水线，包括 OCR、切分、Embedding、Milvus 混合检索和 Reranker。BGE-M3 和 Reranker 属于 PyTorch 生态，放 Python 更合适；业务权限和数据在 Java 微服务体系里，所以 Java 做入口。

### 6.6 离职原因怎么说？

项目交付完成后进入运维期，实施团队合约到期。我个人还是想继续做开发，尤其想往 Java + AI 应用落地方向深入，所以没有续约，开始准备新的机会。

### 6.7 你在团队里的角色？

后端三人。我直接负责 AI 智能助手本地化落地和设备 IoT 接入，另外两位分别负责账册表单、客户核算、清洁打卡、网关和通知。我对其他模块了解整体链路，主要用于联调和排障。

---

## 七、关键数字口径

只主动讲这些：

| 数据 | 推荐口径 |
|---|---|
| 覆盖范围 | 14 个地市、近 2000 座加油站 |
| 设备规模 | 加油机约 8000 台，IoT 终端是万级规模 |
| 设备上报 | 高峰大约 500-1000 条/s |
| 知识库 | 一万多份内部文档，按操作手册、安全规范、培训课件、故障案例分类 |
| 缓存效果 | 高频问题重复度高，超过一半请求可被缓存或候选问答拦截 |

不要主动讲这些，除非能解释统计口径：

- AI 日均调用 4 万多
- 月末峰值 7.5 万
- DAU 1100-1300
- Reranker 提升 35%

---

## 八、最终主线

这个项目的核心价值不是“我接了一个大模型 API”，而是：

> 把大模型能力接进已有 Java 企业业务系统。业务数据通过 Function Calling 调用，知识库问题走 RAG，体验上用 SSE 流式输出，成本上用 Redis 缓存和工具调用拦截，稳定性上做引用来源、兜底和会话隔离。

这条主线要贯穿所有回答。
