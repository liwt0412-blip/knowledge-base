# 李文韬-项目表述（石化经营分析平台）

## 一、自我介绍

---

面试官好，我叫李文韬，今年32岁，来自湖南长沙。我之前是在某石化公司做后端开发的，在这段时间里我接触并参与了石化行业的后台经营管理平台项目，积累了一定的实战经验。

在技术方面，我掌握了Java生态中的主流框架，包括SpringBoot、SpringCloud、MyBatis和MyBatis-Plus等。熟悉关系型数据库MySQL和非关系型数据库Redis的使用与优化，对微服务架构中常用的组件如Nacos注册中心、Gateway网关以及消息中间件RabbitMQ等有实际的落地经验。此外，我对AIGC技术也有比较深入的了解，熟悉大模型API调用、RAG知识库、Agent开发等，并且有实际的项目场景应用经验。

在AI辅助开发方面，我熟练使用Claude Code、Cursor等工具提升开发效率。

在项目经历方面，我最近参与的是中国石化某省公司的经营分析平台项目。接下来我详细介绍一下这个项目的情况。

## 二、项目介绍

---

我参与的这个项目叫**石化经营分析平台**，是中国石化为了提升加油站和便利店经营管理效率而搭建的一套后台管理系统。平台面向省公司、地市公司、片区和油站四级用户，覆盖油站设备管理、账册表单、清洁安环、客户核算、AI智能助手等业务模块。

平台的主要使用者包括：省公司运营管理人员、地市财务人员、片区经理、油站站长、营业员和安全巡检员等。

## 三、项目整体业务流程

---

### 1、加油机加油枪设备管理流程

地市公司设备管理员在系统中维护加油站的基础设备档案，包括油站信息、加油机编号、加油枪编号等。设备数据通过MQTT协议上报至平台，系统实时监控设备状态（正常/异常/离线），异常情况触发告警通知相关油站负责人。

### 2、加能站账册表单管理流程

各油站营业员每日在系统中填报经营数据，包括零售销量、非油商品进销存、HSE安全巡检记录、液位仪比对数据等。数据逐级汇总至片区、地市、省公司，支持报表导出和经营分析。

- 零售管理：记录每日各油枪的销量、油品库存变化
- 非油管理：便利店商品从入库、调拨、促销到盘点的全链路管理，含商品保质期预警
- HSE管理：记录安全巡检、风险评估、铅封检查等
- 数质量管理：液位仪比对、日损耗分析、加油机校验等
- 人事管理：油站考勤、排班、员工花名册
- 财务管理：营业款交接、发票管理

### 3、加油站清洁打卡流程

运营人员在系统中对清洁任务进行排班，员工到站后扫码打卡完成清洁任务，管理人员可查询打卡记录和清洁完成率，确保各站卫生达标。

### 4、单站核算与客户管理流程

各油站建立客户档案（加油卡客户、小额配送客户、石化钱包用户），定期做客户分析，统计客户消费频次、贡献度等指标，支持"六进"等营销活动的客户筛选和效果跟踪。

### 5、AI助手使用流程

油站工作人员在遇到业务操作疑问或系统故障时，可在平台中直接向AI助手提问。AI助手基于知识库和模型推理，提供业务操作指引、常见问题解答和运维建议，降低人工咨询成本。

## 四、项目用到的技术

---

这个项目主要用到的技术栈有：

1. **核心框架：** Spring Boot + Spring Cloud（微服务架构）+ Spring MVC
2. **服务注册与发现：** Nacos
3. **网关：** Spring Cloud Gateway
4. **缓存服务：** Redis（缓存油站基础数据、客户档案热点数据、设备状态）
5. **消息队列：** RabbitMQ（异步处理设备上报数据、订单状态变更通知）
6. **持久层：** MyBatis-Plus（操作 MySQL 数据库）
7. **定时任务：** XXL-Job（处理设备离线检测、报表定时生成、数据同步）
8. **工具：** Hutool（常用工具类）、Jackson（JSON 解析）、Lombok（简化代码）、JWT（令牌认证）

这个项目主要由以下几个微服务组成：

1. **油站管理服务：** 管理加油站、加油机、加油枪等基础设备档案
2. **账册服务：** 处理零售、非油、HSE、数质量、人事、财务等各类经营数据的填报、汇总与查询
3. **核算服务：** 管理客户档案、潜在客户、客户分析清单、营销活动
4. **AI助手服务：** 提供业务操作顾问、系统运维顾问、智能答题等功能
5. **清洁打卡服务：** 处理清洁任务排班、扫码打卡、记录查询
6. **网关服务：** 统一入口，路由转发，鉴权过滤
7. **通知服务：** 处理设备告警、任务提醒等消息推送

## 五、我的职责

---

在这个项目中我主要负责了四个模块，分别是：设备管理服务，非油管理服务，客户核算服务以及AI助手服务。

### 模块一：设备管理服务——加油机物联网设备接入

---

#### 1、业务介绍

设备管理服务负责全省加油站的加油机、加油枪设备档案管理以及物联网数据接入。加油站是石油公司最核心的资产，设备运行状态直接影响经营数据采集的准确性和及时性。

核心功能包括：
- **设备档案管理：** 按"地市→片区→油站→加油机→加油枪"五级维度维护设备信息
- **设备数据采集：** 通过消息队列接收加油机上报的实时数据（加油量、液位仪数据、设备状态等）
- **设备状态监控：** 实时检测设备在线/离线状态，异常时触发告警通知
- **设备校验管理：** 记录加油机定期校验结果，管理铅封、防水滤芯更换等

#### 2、表设计

| 表名 | 说明 |
|------|------|
| **oil_station** | 油站表（油站编码、名称、地市、片区、地址等） |
| **fuel_machine** | 加油机表（关联油站ID、机器编号、型号、状态） |
| **fuel_gun** | 加油枪表（关联加油机ID、枪号、油品类型、状态） |
| **device_data** | 设备上报数据表（油站ID、设备ID、数据类型、数值、上报时间） |
| **device_alert** | 设备告警记录表（油站ID、设备ID、告警类型、告警内容、处理状态） |
| **calibration_record** | 加油机校验记录表（设备ID、校验时间、误差值、校验人） |

#### 3、接口设计

| 接口名称 | 新增加油枪 |
|---------|-----------|
| 请求地址 | `/api/device/gun/add` |
| 请求方法 | POST |
| 请求参数 | `{ "stationCode": "430101", "machineNo": "01", "gunNo": "01", "fuelType": "92#汽油", "status": 1 }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "gunId": 1001 } }` |

| 接口名称 | 查询油站设备列表 |
|---------|---------------|
| 请求地址 | `/api/device/station/list` |
| 请求方法 | GET |
| 请求参数 | `?cityId=4301&page=1&size=10` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "total": 50, "records": [{ "stationCode": "430101", "stationName": "中心加油站", "machineCount": 4, "gunCount": 16, "status": 1 }] } }` |

| 接口名称 | 查询设备实时状态 |
|---------|---------------|
| 请求地址 | `/api/device/status/realtime` |
| 请求方法 | GET |
| 请求参数 | `?stationCode=430101` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "onlineCount": 4, "offlineCount": 0, "alertCount": 1, "machines": [{ "machineNo": "01", "status": 1, "lastReportTime": "2026-06-17 10:00:00" }] } }` |

| 接口名称 | 处理设备告警 |
|---------|------------|
| 请求地址 | `/api/device/alert/process` |
| 请求方法 | POST |
| 请求参数 | `{ "alertId": 2001, "status": 1, "processResult": "已安排维修人员现场检修" }` |
| 响应示例 | `{ "code": 200, "msg": "success" }` |

#### 4、技术实现

1. **设备数据对接与消息处理：**

    加油机通过MQTT协议将实时数据上报到平台的数据接收层。数据接收层对外暴露HTTP接口`/api/device/data/receive`接收JSON格式的加油量、液位仪、设备心跳等数据。接收到数据后，通过RabbitMQ进行异步分发——一条消息写入设备数据表用于历史查询和报表统计，另一条消息进入实时处理队列用于设备状态判断和告警检测。使用消息队列的好处是削峰填谷：全省几千台加油机在早晚高峰集中上报时，通过队列缓冲保证系统不会被打垮。

2. **设备状态判断与告警：**

    设备管理服务维护一个Redis缓存（key: `device:heartbeat:{deviceId}`, TTL: 5分钟），每次收到设备上报数据时更新该缓存。同时通过XXL-Job定时任务每隔5分钟扫描所有设备，检查哪些设备最近5分钟内没有上报心跳，标记为离线并生成告警记录。告警记录通过RabbitMQ发送给通知服务，通知服务通过系统消息推送给对应油站负责人。

    Redis的TTL机制天然适合做设备超时判断——设备只要持续上报，TTL不断续期；一旦中断，TTL到期后Redis自动过期，定时任务扫描时就能检测到，比遍历数据库判断时间差效率高得多。

3. **统一返回格式与异常处理：**

    前后端所有接口统一使用R对象返回格式。通过全局异常处理器`@ControllerAdvice`统一拦截业务异常和系统异常，避免每个Controller重复写try-catch。前端拿到统一的响应结构后，可以在Axios拦截器中统一处理错误提示和登录超时跳转，开发效率提升不少。

4. **MyBatis-Plus数据操作：**

    CRUD操作全部基于MyBatis-Plus的BaseMapper和IService，分页使用MyBatis-Plus自带的分页插件。公共字段（createTime、updateTime、createUser、updateUser）通过MyBatis-Plus的自动填充功能处理，业务代码里不需要手动赋值。

#### 5、为什么使用这个技术

- **RabbitMQ + Redis组合：** 加油站设备上报数据量大且有明显的波峰波谷特征（早晚加油高峰），用消息队列缓冲是标准做法。Redis做设备状态判断则是因为TTL机制天然适合"心跳超时"场景，比数据库轮询更高效。
- **MyBatis-Plus：** 油站管理涉及大量CRUD操作（新增油站、编辑加油机、查询设备列表等），MyBatis-Plus的代码生成器和BaseMapper能减少大量重复代码，核心业务用XML写复杂SQL，简单CRUD用注解搞定。

#### 6、存在什么问题

- **设备数据延迟：** 加油机通过MQTT上报到平台再经RabbitMQ流转，对于需要实时告警的场景（如加油机异常停机）可能延迟几十秒。
- **网络不稳定：** 部分偏远站点网络条件差，偶尔出现长时间断连，导致上报数据丢失。
- **数据一致性：** 加油机的累计加油量和系统统计量偶尔不一致，需要定时对账校准。

#### 7、如何解决

- **设备告警双通道：** 对于紧急告警，除了消息队列异步处理外，增加一条直接写入告警表的同步通道，确保关键告警不丢失。
- **断线重连补偿：** 设备重新上线后，自动补传离线期间的关键数据（如总加油量、液位仪读数），通过RabbitMQ的延迟队列实现。
- **日终对账任务：** 通过XXL-Job每天凌晨跑对账任务，将系统统计的日销量与加油机上报的累计销量进行比对，差异超阈值时标记异常并通知运营人员核查。

---

### 模块二：非油管理服务——便利店商品全链路管理

---

#### 1、业务介绍

非油管理服务负责全省加油站便利店商品的全链路管理。加油站便利店虽然面积不大，但SKU种类多（食品、饮料、润滑油、日用品等），而且涉及多个地市多个油站之间的调拨和库存管理，业务流程复杂度不低。

核心功能包括：
- **商品信息管理：** 维护便利店商品的档案信息（名称、条码、分类、售价、保质期等）
- **商品调拨审批：** 油站之间或地市仓到油站的商品调拨申请、审批、出库、入库全流程
- **便利店商品盘点：** 油站定期盘点，系统根据盘点结果自动生成盘盈盘亏记录
- **商品损耗管理：** 记录商品报损（过期、破损等），统计分析损耗率
- **商品促销管理：** 配置促销活动（满减、折扣、赠品），跟踪促销效果
- **赠品台账：** 管理赠品的入库、领用、库存
- **卫生清洁台账：** 记录便利店卫生检查情况

#### 2、表设计

| 表名 | 说明 |
|------|------|
| **product** | 商品表（商品名称、条码、分类、售价、成本价、保质期天数） |
| **inventory** | 库存表（油站ID、商品ID、库存数量、预警阈值） |
| **transfer_order** | 调拨单表（调出油站、调入油站、调拨状态、审批人） |
| **transfer_item** | 调拨明细表（关联调拨单、商品ID、数量） |
| **check_record** | 盘点记录表（油站ID、盘点时间、盘点人） |
| **check_item** | 盘点明细表（商品ID、账面数量、实际数量、差异） |
| **waste_record** | 损耗记录表（商品ID、数量、损耗原因、处理方式） |
| **promotion** | 促销活动表（活动名称、开始时间、结束时间、类型、规则） |

#### 3、接口设计

| 接口名称 | 新增商品 |
|---------|---------|
| 请求地址 | `/api/nonoil/product/add` |
| 请求方法 | POST |
| 请求参数 | `{ "productName": "农夫山泉550ml", "barcode": "6901234567890", "categoryId": 101, "salePrice": 2.00, "shelfLife": 365 }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "productId": 5001 } }` |

| 接口名称 | 发起商品调拨 |
|---------|------------|
| 请求地址 | `/api/nonoil/transfer/create` |
| 请求方法 | POST |
| 请求参数 | `{ "fromStationCode": "430101", "toStationCode": "430102", "items": [{ "productId": 5001, "quantity": 100 }], "reason": "库存不足" }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "transferId": 3001, "status": 0 } }` |

| 接口名称 | 审批调拨单 |
|---------|----------|
| 请求地址 | `/api/nonoil/transfer/approve` |
| 请求方法 | POST |
| 请求参数 | `{ "transferId": 3001, "approveStatus": 1, "approveRemark": "同意调拨" }` |
| 响应示例 | `{ "code": 200, "msg": "success" }` |

| 接口名称 | 查询商品库存（分页） |
|---------|-----------------|
| 请求地址 | `/api/nonoil/inventory/list` |
| 请求方法 | GET |
| 请求参数 | `?stationCode=430101&categoryId=101&page=1&size=20` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "total": 150, "records": [{ "productId": 5001, "productName": "农夫山泉550ml", "quantity": 500, "alertThreshold": 50, "status": "normal" }] } }` |

| 接口名称 | 提交盘点结果 |
|---------|-----------|
| 请求地址 | `/api/nonoil/check/submit` |
| 请求方法 | POST |
| 请求参数 | `{ "stationCode": "430101", "items": [{ "productId": 5001, "expectedQty": 500, "actualQty": 498, "diff": -2 }] }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "checkId": 4001, "diffCount": 2, "diffAmount": -4.00 } }` |

#### 4、技术实现

1. **商品管理CRUD：**

    商品管理模块是典型的增删改查，基于MyBatis-Plus的BaseMapper实现，分页查询使用MyBatis-Plus的分页插件PageHelper。商品分类使用树形结构存储（parent_id方式），前台展示时通过递归查询构建分类树。商品条码做了唯一索引，防止重复录入。

2. **调拨审批流：**

    调拨申请提交后状态变为"待审批"，通过Gateway网关根据当前用户的角色判断是否有审批权限。审批通过后，系统自动更新调出方库存（减库存）和调入方库存（加库存），这个操作放在同一个事务中保证数据一致性。如果审批拒绝，调拨单状态变为"已拒绝"，库存不做变动。

3. **盘点流程与差异处理：**

    盘点时先锁定油站库存（防止盘点期间有销售出库），生成盘点底稿。员工逐项录入实际数量后提交，系统比对账面数量和实际数量，自动计算差异。差异在合理范围内（如损耗率<0.5%）自动通过审核，超范围的需要管理员人工确认并填写原因。

4. **保质期预警：**

    通过XXL-Job每天凌晨扫描所有商品，查询距离保质期到期不足30天的商品，生成预警推送给对应油站负责人。同时支持按商品分类、油站维度查询临期商品清单，方便运营人员处理（降价促销或报损）。

#### 5、为什么使用这个技术

- **MyBatis-Plus：** 非油管理接口绝大部分是单表CRUD加简单关联查询，MyBatis-Plus基本不需要写SQL，开发效率高。分页查询也是框架自带的，省去自己写limit/offset的麻烦。
- **事务保证数据一致性：** 调拨审批通过后的库存变更涉及两张表（调拨单状态更新 + 双方库存更新），放在一个事务里执行，防止出现"扣了库存但没加到对方"的情况。
- **定时任务做预警：** 保质期预警这种场景不需要实时触发，每天凌晨跑一次就够了，XXL-Job支持分布式部署，比Spring自带的@Scheduled更可靠。

#### 6、存在什么问题

- **并发调拨冲突：** 同一个商品同时被多个调拨单操作时，可能出现库存超扣的情况。
- **盘点期间业务中断：** 盘点时需要锁定库存，盘点期间该油站不能有销售出库，影响正常业务。
- **临期商品处理不及时：** 预警虽然是每天推送，但油站负责人可能忽略，导致商品过期报损。

#### 7、如何解决

- **乐观锁防止超扣：** 库存更新时使用MyBatis-Plus的乐观锁机制（version字段），更新时校验version，版本不一致则更新失败，提示重试。
- **盘点窗口优化：** 将盘点时间安排在凌晨低峰期（通过XXL-Job定时盘点），避开白天营业高峰。同时支持"动盘"模式（只盘点有变动的商品，不锁定全部库存）。
- **临期商品升级提醒：** 对于距离保质期不足7天的商品，预警等级升级，通过短信或企业微信消息直接通知油站站长，不再仅仅依赖系统内消息。

---

### 模块三：客户核算服务——客户档案与经营分析

---

#### 1、业务介绍

客户核算服务负责全省加油站客户信息的统一管理和经营数据分析。石化公司不仅要管"油"，还要管"客户"——特别是大客户（物流公司、车队、企业客户）的消费情况和信用额度。

核心功能包括：
- **客户档案管理：** 创建和维护客户档案（加油卡客户、小额配送客户、石化钱包用户），包括基本信息、信用额度、合同信息
- **潜在客户管理：** 记录潜在客户信息，跟踪开发进度，支持批量导入
- **客户分析清单：** 按消费频次、消费金额、活跃度等维度生成客户分析报表
- **六进活动管理：** 管理"进企业、进社区、进机关"等营销活动，记录活动效果和客户转化情况

#### 2、表设计

| 表名 | 说明 |
|------|------|
| **customer** | 客户表（客户名称、类型、联系方式、信用额度、状态） |
| **customer_contract** | 客户合同表（合同编号、客户ID、合同金额、有效期） |
| **potential_customer** | 潜在客户表（企业名称、联系人、电话、来源、开发状态） |
| **customer_analysis** | 客户分析数据表（客户ID、统计月份、消费金额、消费次数） |
| **marketing_activity** | 营销活动表（活动名称、类型、开始时间、结束时间、参与油站） |
| **activity_result** | 活动效果表（关联活动ID、新增客户数、销售额、投入成本） |

#### 3、接口设计

| 接口名称 | 创建客户档案 |
|---------|------------|
| 请求地址 | `/api/customer/create` |
| 请求方法 | POST |
| 请求参数 | `{ "customerName": "XX物流公司", "customerType": "fleet", "contactPerson": "张三", "contactPhone": "13800138000", "creditLimit": 500000 }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "customerId": 6001 } }` |

| 接口名称 | 查询客户分析报表 |
|---------|---------------|
| 请求地址 | `/api/customer/analysis/list` |
| 请求方法 | GET |
| 请求参数 | `?month=2026-06&page=1&size=20&sortBy=amount&sortOrder=desc` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "total": 200, "records": [{ "customerName": "XX物流公司", "totalAmount": 150000, "totalCount": 45, "avgAmount": 3333, "activeDays": 22, "rank": 1 }] } }` |

| 接口名称 | 新增潜在客户 |
|---------|-----------|
| 请求地址 | `/api/customer/potential/add` |
| 请求方法 | POST |
| 请求参数 | `{ "companyName": "XX运输公司", "contactPerson": "李四", "source": "六进活动-进企业", "status": "contacting" }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "potentialId": 7001 } }` |

| 接口名称 | 创建营销活动 |
|---------|-----------|
| 请求地址 | `/api/customer/activity/create` |
| 请求方法 | POST |
| 请求参数 | `{ "activityName": "6月进企业推广活动", "activityType": "enterprise", "startTime": "2026-06-01", "endTime": "2026-06-30", "stations": ["430101", "430102"] }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "activityId": 8001 } }` |

#### 4、技术实现

1. **客户档案CRUD：**

    客户档案管理也是标准的增删改查，使用MyBatis-Plus实现。客户类型用字典表维护（加油卡、小额配送、石化钱包），支持模糊搜索和分页查询。客户信用额度变更时，需要记录变更日志，方便财务审计追溯。

2. **客户分析报表：**

    客户分析数据不是实时计算的，而是通过XXL-Job每天晚上统计当天的消费数据，按客户维度汇总后存入客户分析数据表。查询时直接从汇总表取数，避免每次查询都对大量交易记录做实时聚合。支持按消费金额、消费次数、活跃天数等维度排序和筛选。

    报表接口使用了MyBatis-Plus的分页查询，排序字段通过参数动态传入，避免了硬编码多个查询方法。SQL层面做了索引优化——在客户ID和统计月份上建了联合索引，查询效率基本是毫秒级。

3. **六进活动管理：**

    活动管理包含活动创建、活动执行跟踪和活动效果统计三个环节。活动创建时选择参与油站和活动类型，活动结束后运营人员录入效果数据（新增客户数、销售额等），系统自动计算投入产出比（ROI），为后续活动预算提供数据参考。

#### 5、为什么使用这个技术

- **汇总表替代实时聚合：** 客户消费明细数据量庞大（全省每天几万笔交易），如果每次查报表都去聚合原始交易数据，数据库扛不住。通过定时任务提前汇总，查询时走汇总表，性能问题基本不存在。
- **MyBatis-Plus + 动态排序：** 客户分析报表的排序维度不固定（有时按金额、有时按频次），用MyBatis-Plus的LambdaQueryWrapper动态拼接排序条件，代码比写多个XML映射简洁得多。

#### 6、存在什么问题

- **数据时效性：** 汇总表是T+1的（隔天更新），运营人员当天下班前查到的客户数据其实是昨天的，不能实时反映当天情况。
- **客户重复问题：** 不同油站录入的客户可能重复（同一个物流公司在多个油站加油），但当时没有做客户去重。
- **活动效果难量化：** 六进活动的新增客户，很难精确归因到某个具体活动（客户可能是多渠道过来的）。

#### 7、如何解决

- **实时+定时双轨：** 在客户分析明细页增加"今日实时"tab，直接查当天的交易记录（限制只查当天，数据量小），首页报表仍用T+1汇总数据，两者互不干扰。
- **客户统一认证：** 基于客户统一社会信用代码或营业执照号做去重，同一企业主体在系统内只保留一条客户记录，各油站通过关联关系引用。
- **活动归因标记：** 客户注册时增加"来源活动"字段，新注册客户必须选择或由系统自动匹配最近参加过的活动，后续分析时按来源活动分组统计。

---

### 模块四：AI助手服务——大模型应用

---

#### 1、业务介绍

AI助手服务为石化平台提供智能化能力支撑，面向三类使用场景：

- **业务操作顾问：** 油站工作人员在操作系统中遇到"如何新建便利店商品"、"怎么填报损耗数据"等问题时，AI助手基于业务知识库给出步骤指引。这对减少油站人员培训成本和降低咨询电话量很有帮助。
- **系统运维顾问：** IT运维人员遇到系统报错或配置问题时，AI助手提供故障排查建议和解决方案。
- **智能答题助手：** 省公司定期组织员工进行安全规范、业务流程等考试，AI助手可以自动根据知识库内容生成考题和答案解析，支撑培训考核。

AI助手采用**RAG（检索增强生成）架构**：将中国石化内部的操作手册、业务流程文档、安全规范等文档进行向量化处理存入向量数据库，用户提问时先检索相关文档片段，再结合大模型生成回答，保证回答内容严格基于公司内部资料，不产生"幻觉"。

#### 2、表设计

| 表名 | 说明 |
|------|------|
| **ai_qa_record** | 问答记录表（用户ID、问题、回答、文档引用列表、评分） |
| **ai_knowledge_base** | 知识库文档表（文档标题、文档内容、向量ID、分类、上传时间） |
| **ai_feedback** | 用户反馈表（记录ID、评价、改进建议） |

#### 3、接口设计

| 接口名称 | AI助手问答 |
|---------|-----------|
| 请求地址 | `/api/ai/assistant/chat` |
| 请求方法 | POST |
| 请求参数 | `{ "userId": 1001, "question": "如何新建便利店商品？", "scene": "business" }` |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "answer": "操作步骤如下：1. 进入非油管理模块…", "references": ["非油管理操作手册V2.3.pdf"], "relatedQuestions": ["如何修改商品信息？", "如何进行商品调拨？"] } }` |

| 接口名称 | 知识库文档上传 |
|---------|--------------|
| 请求地址 | `/api/ai/knowledge/upload` |
| 请求方法 | POST |
| 请求参数 | 表单上传（文件 + 文档分类标签） |
| 响应示例 | `{ "code": 200, "msg": "success", "data": { "docId": 5001, "chunkCount": 25 } }` |

#### 4、技术实现

**4.1 三层检索架构（Redis → MySQL-BM25 → RAG向量检索）**

    这是我设计的一个三级递进检索链路，核心思路是"能快不慢、能用缓存不查库、能用关键词匹配不走向量检索"：

    **第一层——Redis缓存命中：** 用户提问后，先用问题的MD5哈希值作为key去Redis查。如果命中，直接返回缓存答案，响应时间<5ms。这层主要拦截高频重复问题——油站员工问得最多的是"怎么登录""怎么建商品""怎么改密码"这类，缓存命中率在60%以上。

    **第二层——MySQL BM25关键词检索：** Redis没命中，走BM25全文检索。我从项目启动时就把数据库里的热门问答数据加载到内存中的BM25Okapi模型（rank_bm25库），用jieba分词器对问题和用户查询分别分词后计算BM25分数，再通过Softmax归一化得到每个候选问题的匹配概率。设定阈值0.85——超过阈值说明词频高度吻合，直接返回数据库答案，并回写到Redis缓存供下次命中；低于阈值说明数据库里没有现成答案，进入RAG流程。

    **第三层——RAG向量检索+大模型生成：** 前两层都没命中，说明这是一个数据库里没有覆盖到的新问题或复杂问题，这时才走RAG。这也是成本最高的一层——需要调用嵌入模型、向量检索、重排序模型、大模型生成，单次耗时2-5秒。

    这种三层架构的好处是：绝大多数高频简单问题在前两层就解决了，只有真正需要"理解语义"的复杂问题才走RAG。这比"所有问题都走RAG"的方案节省了约70%的大模型调用量。

**4.2 BERT意图分类器——问题路由**

    进入RAG流程前，还有一个前置判断：用户的问题到底需不需要走RAG？如果用户在闲聊或者问一个通用知识问题（如"SpringBoot原理是什么"），直接让大模型回答就行，没必要检索知识库。

    我基于BERT-base-chinese做了一轮微调，训练了一个二分类模型来区分"通用知识"和"专业咨询"。训练数据约1000条人工标注样本，按7:1.5:1.5划分训练/验证/测试集，微调3个epoch。微调前准确率只有48%（跟瞎猜差不多），微调后达到90%以上。分类结果决定后续路由：通用知识走LLM直答通道，专业咨询走RAG检索通道。

    之所以用BERT（0.1B参数）而不是直接用大模型分类，是因为BERT足够小、推理快（<50ms），不会成为链路瓶颈。

**4.3 文档预处理——父子块分层切分策略**

    知识库的核心是文档质量。石化的操作手册少则几十页、多则上百页，直接整篇丢给向量模型效果很差——大模型一次性处理不了太长文本，且未经切分的文档检索精度极低。

    我采用了"父块-子块"双层切分策略：

    - **父块**（900字符）：按分隔符递归切分，保留完整的段落/章节结构。Markdown文档用MarkdownTextSplitter按标题层级切，普通文档用ChineseRecursiveTextSplitter按句号/换行等分隔符递归拆。
    - **子块**（300字符）：在父块基础上进一步切分，保证每个片段粒度足够细，能精准匹配用户问题。

    存储时，子块内容向量化存入Milvus用于精确检索；父块内容作为标量字段关联存储。检索时，用子块匹配到最相似的片段后，返回对应的父块内容作为大模型的上下文——这样既保证了检索精度（小粒度匹配），又保证了上下文完整性（大粒度喂给LLM）。

**4.4 双路混合检索 + 二次重排序**

    这是整个RAG流程的核心——单一的向量检索召回效果有限，我采用了"稠密+稀疏"双路加权混合检索：

    - **稠密向量（语义检索）：** 基于BGE-M3模型对用户问题编码，用IVF_FLAT倒排索引在Milvus中做ANN近似最近邻搜索（nprobe=10），找到"意思相近"的文档。
    - **稀疏向量（词汇匹配）：** BGE-M3同步生成的稀疏向量（本质是词频权重向量），用SPARSE_INVERTED_INDEX索引做词汇级匹配，找到包含"相同关键词"的文档。
    - **加权融合：** 稠密路径权重1.0、稀疏路径权重0.7，各取Top-K结果后通过Milvus的WeightedRanker合并排序。稠密权重大于稀疏，是因为石化业务场景下用户措辞多变（"建商品"可能说成"新增货物""加个商品"），语义理解比关键词匹配更重要。

    两路召回后再做一次二次重排——用BGE Reranker Large交叉编码器模型，把用户问题和每个召回的父块内容拼成(query, doc)对逐一打分，按相关性分数重排后取Top-M返回。这一步相当于让一个专门的"裁判"再筛一遍，把最相关的内容排到最前面。实验表明，加Reranker后Top-5的相关性提升了约35%。

**4.5 多格式文档OCR加载**

    石化业务文档格式很杂：有Word操作手册、PDF规章制度、PPT培训课件，甚至还有纸质的巡检表扫描件。我封装了统一的文档加载器，根据不同文件后缀自动路由到对应的加载器（PDF用OCRPDFLoader、Word用OCRDOCLoader、PPT用OCRPPTLoader、图片用OCRIMGLoader），底层基于PaddleOCR做光学字符识别提取文字，确保图文混排的文档也能完整提取。

**4.6 三类Prompt差异化设计**

    虽然底层都是同一套RAG架构，但三个助手的角色完全不同，Prompt必须分开设计：
    
    - **业务操作顾问：** 要求严格按文档步骤输出，"第1步做什么、第2步做什么"，不允许自由发挥。因为业务操作出错会导致实际损失（如盘点错、调拨错）。
    - **系统运维顾问：** 允许结合故障案例给出排查建议，输出格式固定为"问题→可能原因→解决方案"，帮助运维人员快速定位。
    - **智能答题助手：** 基于知识库内容自动生成考题，输出格式固定为"题干+4个选项+正确答案+解析"，支撑公司安全培训考试。

    三个Prompt都注入了兜底逻辑：如果上下文不足以回答，返回"请联系人工客服"而不是强行编造——这条规则在实际使用中非常重要，因为石化业务涉及安全规范，错误回答可能造成严重后果。

**4.7 SSE流式输出 + 会话历史管理**

    大模型生成回答通常需要2-5秒，如果等全部生成完再返回，用户体验很差。我实现了SSE（Server-Sent Events）流式输出，前端每收到一个token就逐字渲染，让用户感知到系统"在打字"而不是"卡住了"。

    同时做了会话历史管理：每个会话保留最近5轮对话存入MySQL的conversations表，RAG生成回答时将历史拼入Prompt，让AI助手能理解上下文——比如用户先问"怎么建商品"，再问"那建完之后呢"，AI能理解"建完之后"指"建完商品之后"。

**4.8 选型决策：RAG vs 微调、SpringAI vs 直接调用**

    - **选RAG不选微调：** 石化业务文档频繁更新（油价调整通知、新安全规范下发），RAG只需替换知识库文件即可生效，迭代成本为零。微调则需要重新标注数据、重新训练，每次更新成本高。而且RAG能引用文档来源："这段回答来自《非油管理操作手册V2.3》"，可信度远远高于微调模型。
    - **选SpringAI统一模型调用：** 用SpringAI的ChatClient封装大模型API调用，而不是直接调OpenAI SDK。好处是后续切换模型供应商（如从通义千问换成DeepSeek）只需改配置文件，代码零改动。

#### 5、为什么使用这个技术

- **三层递进检索而非全部走RAG：** 油站员工问的大多是重复问题，三层架构让60%的问题在前两层就解决了，大模型调用量降低70%，直接控制住了运营成本。
- **RAG而非微调：** 石化业务文档频繁更新（油价调整、新政策下发），RAG模式下只需更新知识库文档，不需要重新微调模型，迭代成本极低。而且RAG能明确告诉用户"这段回答来自哪份文档"，可信度更高。
- **双路混合检索而非单路：** 稠密+稀疏双路互补——稠密向量能理解"建商品"= "新增货物"（语义），稀疏向量能精准匹配"油品""损耗"等专业术语（词频），两者取长补短。加了Reranker二次重排后，Top-5相关性提升约35%。
- **SpringAI框架：** 切换模型提供商只需要改配置，代码几乎不用动。如果后续换成DeepSeek或其他大模型，一行代码都不用改。
- **SSE流式输出：** 2-5秒的生成等待中逐字渲染，杜绝用户"以为卡住了"的焦虑感。

#### 6、存在什么问题

- **知识库更新延迟：** 业务文档更新后，如果管理员忘记上传新版本，AI助手给的还是旧文档的答案，可能误导员工。这是RAG架构的固有问题——知识库不会自动同步。
- **检索精度边界：** 部分操作流程比较接近（如"新建商品"和"新建促销"），偶尔检索到不相关的文档片段。虽然加了Reranker，但交叉编码器的判别能力也有上限。
- **成本控制：** 云端大模型按token计费，每次RAG调用消耗约2000-4000 token。三层架构已经过滤掉70%，但剩余30%的RAG调用仍需持续关注成本。一个优化方向是对高频RAG问题也做Redis缓存（缓存的是最终回答而非中间结果）。
- **BERT分类器误判：** 测试集准确率90%，意味着还有10%的问题会被错误路由——专业问题被当成通用问题直接让LLM回答，可能产生"幻觉"。

#### 7、如何解决

- **知识库版本管理：** 在文档管理后台增加版本标记和生效日期字段，AI助手优先回答生效日期内的文档。同时在管理端增加"知识库健康度"看板，监控文档更新状态和覆盖率。
- **检索优化——Hybrid Search增强：** 目前的双路混合检索权重是固定的（稠密1.0/稀疏0.7）。后续可以引入自适应权重：对关键词密集型问题自动调高稀疏权重，对描述型问题调高稠密权重，进一步提升召回精度。
- **RAG回答缓存：** 对高频RAG问题的最终回答做Redis缓存（TTL=24小时），同类问题直接返回缓存结果，进一步降低大模型调用成本。
- **分类器升级：** BERT二分类只能区分通用/专业，但业务顾问、运维顾问、答题助手三个场景的问题可能需要不同的检索策略。后续可以考虑用更大参数的模型做多分类，或引入规则引擎辅助判断，降低10%误判率。
