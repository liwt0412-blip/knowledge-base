---
tags: [Java]
date: 2026-06-10
---

# 文件上传


## 本地存储

### 通过MutipartFile这个类型的参数可以接收前端上传过来的文件（注意前端的表单标签必须加上enctype="multipart.form-data"才生效，否则只能接收到文件名）

### 前端上传过来的所有参数都会先存放在本地的临时目录下，一旦请求响应完毕，临时目录下的文件就会被清空

### 将前端传过来的文件保存到本地

#### 1. 定义一个File对象

#### 2. 使用transferTo方法保存文件到File路径

##### file.transferTo(File destination)

#### 3. 注意路径名可能会重复，所以在保存前要做好随机命名工作

##### file.getOriginalFileName()

##### originalFilename.substring(originalFilename.lastIndexOf("."))

##### UUID.randomUUID.toString().replace("-","")

### 文件传输大小限制

#### 默认单个文件大小为1M

#### spring:servlet:multipart:max-file-size:10MB

##### 最大单个文件大小

#### spring:servlet:multipart:max-request-size:100MB

##### 最大请求大小

## 阿里云OSS

### 概念

#### 对象存储OSS（Object Storage Service）

#### 软件开发工具包SDK（Software Development Kit）

#### Bucket：存储空间是用户用于存储对象的容器，所有的对象都必须隶属于某个存储空间

### 部署

#### 官网：https://www.aliyun.com/

#### 注册 — 实名认证 — 登录 — 充值

#### 开通对象存储服务

#### 创建一个Bucket

##### 输入名称、地域、标准存储类型、同城冗余存储（推荐）——完成创建

##### 概览——权限控制——关闭“阻止公共访问”——设置读写权限为“公共读”

#### 配置AccessKey（为了可以调用阿里云的服务）

##### 点击图像——AccessKey——创建AccessKey——保存AccessKeyID和Secret

##### 以管理员身份打开CMD命令，执行如下命令，配置系统的环境变量

###### set OSS_ACCESS_KEY_ID=xxxxxxxxxxxxxxxxxx

###### set OSS_ACCESS_KEY_SECRET=xxxxxxxxxxxxxxxxx

##### 执行如下命令，让更改生效（分条输入）

###### setx OSS_ACCESS_KEY_ID "%OSS_ACCESS_KEY_ID%"

###### setx OSS_ACCESS_KEY_SECRET "%OSS_ACCESS_KEY_SECRET%"

##### 执行如下命令，验证环境变量是否生效（分条输入）

###### echo %OSS_ACCESS_KEY_ID%

###### echo %OSS_ACCESS_KEY_SECRET%

### 参照官方文档编写入门程序

#### 引入依赖aliyun-sdk-oss

##### <dependency>
       <groupId>com.aliyun.oss</groupId>
       <artifactId>aliyun-sdk-oss</artifactId>
       <version>3.17.4</version>
</dependency>

#### 复制官方入门程序，并本地化其中参数

##### 获取文件对象的字节数组

###### Files.readAllBytes(file.toPath())

#### 优化代码

##### 上传的文件按照月份存储，每个月存入一个文件夹

##### 抽取配置信息

###### 法一

###### 将参数配置到配置文件中

###### aliyun:
  oss:
    endpoint: https://oss-cn-beijing.aliyuncs.com
    bucketName:  qk-web01
    region: cn-beijing

###### 将配置信息注入到阿里云操作类的属性上

######     @Value("${aliyun.oss.endpoint}")
    private String endpoint;
    @Value("${aliyun.oss.bucketName}")
    private String bucketName = "qk-web01";
    @Value("${aliyun.oss.region}")
    private String region = "cn-beijing";

###### 法二

###### 定义一个阿里云实体类AliyunOSSProperties

###### 注意实体类里面的属性名要与配置文件里的名字完全相同

######     private String endpoint;
    private String bucketName;
    private String region;

###### 在阿里云实体类上添加@Data@Component

###### 添加@ConfigurationProperties(prefix = "aliyun.oss")

###### 在阿里云操作类里注入阿里云实体类对象

## 相关笔记

- [[☕ Java笔记/Java笔记总览|Java笔记总览]]


- [[MOC-Java基础]]
