# 项目记录

> 本文档整理面试用项目表述，包含三个项目：天机学堂（在线教育微服务）、黑马点评（本地生活服务）、启山智软 SmartShop（电商中台）。
> 格式参考：[[天机学堂/课程资料/17-项目表述]]

---

## 一、天机学堂项目

### 项目介绍

天机学堂是一个基于微服务架构的生产级在线教育项目，面向成年人的非学历职业技能培训平台。分为两部分：
- **学生端**：核心业务主体是学员，所有业务围绕学员展开
- **管理端**：核心业务主体包括老师、管理员、其他员工，核心业务围绕老师展开

项目中包含学习服务、课程服务、点赞服务、优惠券服务、搜索服务等12个微服务。

### 技术栈

SpringCloud Alibaba、SpringBoot、SSM、MyBatis-Plus、Redis/Redisson、RabbitMQ、MySQL分库分表、XXL-Job、Nacos、Gateway、Feign、Seata、Sentinel、Elasticsearch、Jenkins + Docker

### 项目负责（核心功能）

1. 负责实现学习服务，基于Redis合并写请求和DelayQueue实现断点续播功能，优化高并发的数据库写业务，使误差控制在15秒内。
2. 独立设计并实现问答评论模块，根据用户选择的匿名与否，记录问答或者评论信息，并产生对应的积分，通过RabbitMQ推送给积分系统。
3. 独立负责点赞服务的搭建与实现，用户前端发送点赞请求到达点赞服务，点赞服务负责存储点赞记录与点赞数量，并通过定时任务发送MQ消息通知业务方更新点赞数量。
4. 负责签到功能实现与优化，基于BitMap数据结构存储签到记录，通过MQ推送签到信息及积分到积分服务。
5. 负责积分排行榜功能实现，利用Redis的Zset存储本月实时排行榜数据。通过XXL-Job分片任务持久化历史榜单到MySQL数据库中，MySQL采用的是分库分表，利用MybatisPlus的动态表名来计算表名。
6. 负责优惠券管理功能，发放优惠券时利用按位加权求和算法和异步线程生成兑换码，并利用BitMap验证兑换码是否已经兑换。保证了兑换码的可读性、不可重兑、防爆刷和高效性。
7. 负责优惠券的领取功能，利用乐观锁和分布式锁Redisson解决并发安全问题，解决事务边界原因导致的锁失效问题，通过AspectJ动态代理解决事务失效问题。
8. 负责实现通用分布式锁的封装，基于AOP、自定义注解、工厂模式和策略模式实现。
9. 负责优惠券的使用，通过查询所有可用优惠券，经过初筛、细筛、排列组合计算出所有可用优惠券组合，并基于CompletableFuture并行计算每种组合的优惠明细，最后按照规则筛选出最优解。

### 项目表述

> 在讲的过程中，可以先讲一部分引起面试官兴趣，再以一问一答的方式进行，不需要一股脑把所有技术实现流程都讲出来。

#### 1. 断点续播功能

**业务介绍**

用户购买课程后，会发送MQ，课程会加入到课表中，在个人中心学习界面用户就能进行学习课程。用户点击马上学习，会进入视频播放的界面，一个课程中包含很多章节视频，一般最后一个小节是考试。前端会每隔15秒提交一次播放记录，主要就是提交播放章节、播放的进度、小节类型、提交的时间等参数。我们需要在后台记录下这些信息，方便用户断点续播。并且我们规定好了，如果学习进度超过50%，代表用户已经学完该小节。

**实现流程**

在用户提交学习记录到后台，首先会判断一下小节类型，也就是判断本次提交的是视频还是考试。

如果是考试比较简单，考试只提交一次就行，代表用户已经学完这个小节，所以我们直接在学习记录表中新增一条记录，并且状态是已学完，再到课表中更新已学习的小节数量+1，还要判断下是否学完全部小节（通过远程调用课程服务查询全部小节，与课表中的已学习小节进行比较），如果全部学完，更新课表的状态为已学完。

如果是视频稍微麻烦一点，因为用户每隔15秒提交一次，如果同时在线的用户比较多并发量就会很高，而且在播放的过程中其实只需要给数据库中记录用户最后一次播放进度就行，前面的播放记录会被覆盖，所以会造成很多无效的写操作。这里我们使用**Redis来合并写请求**，并使用**延迟任务DelayQueue**来判断用户是否还处于播放中。

当用户提交过来视频播放记录后，我们首先会查询Redis缓存判断播放记录是否存在，如果不存在会直接新增一个学习记录。如果记录存在，会判断是不是第一次学完，如果不是的话（这种请求会占用整个视频请求的90%以上），我们就会缓存到Redis中，同时会提交一个20秒之后执行的延迟任务。

在项目启动的时候我们就会开启一个异步任务，不停的检测延迟队列中是否有任务，一旦有任务，就会判断这里的任务和Redis中的学习记录是否一致：
- 如果一致，也就是说用户已经超过20秒没有再提交新的学习记录，说明用户已经停止播放了，这里我们就会持久化到数据库中，更新学习记录和课表中最近学习小节和时间。
- 如果不一致，说明用户还在持续播放，直接结束不做处理。

另外如果经过判断是第一次学完，也会直接更新数据库的学习记录，同时还要清理Redis缓存（数据库与缓存保持一致），更新课表已学习的小节数量+1，判断是不是全部学完，如果是再更新课表状态。

所以虽然用户端不停的提交播放进度，但是优化后这里写数据库的频率就会少很多：用户第一次过来新增学习记录，第一次学完会更新学习状态，中途停止播放也会更新数据库。

**可能会追加提问**

- Redis使用的是哪种数据结构来记录的？key和value是如何设计的？
- 延迟任务为什么使用的是DelayQueue？有什么优点？还有其他延迟任务可供选择吗？
- 项目启动时的异步任务是如何实现的？
- 高并发问题一般还有哪些优化解决方案？

---

#### 2. 互动问答功能（着重讲清楚表设计）

**业务介绍**

用户在学习的过程中，可以在播放界面的侧边进行提问，其他用户如果看到这个问题可以对这个问题进行回答，并且也可以对回答进行评论。在管理端，管理员也可以查看这个问题，同样也可以对这个问题进行回答或者评论。并且管理端还有隐藏功能，可以对问题、回答或者评论进行隐藏。

**实现流程**

我先说一下这里的表结构。这里设计了两张表，分别是**问题表**和**回答评论表**。

问题表主要记录的是问题所属的课程、章、节，提问者的id，是否匿名，是否隐藏，问题内容，回答的数量，还有最近一次的回答信息等。因为我们在查询问题列表的时候需要展示最近一次回答者的信息，所以最近回答的也会被记录在问题表中。

回答表主要记录的是问题的id，回答者的id，回答内容，是否匿名，是否隐藏，点赞数量、评论数等，还有上级的回答id，评论的目标id和目标用户。因为回答和评论是属于一张表，并且业务上可以对回答评论，可以继续对评论进行评论，所以是用**自关联**的方式记录了目标id。

我先说一下新增评论或者回答吧。用户提交过来回答内容，是否匿名，上级的id，目标用户id这些之后，我们除了直接保存到回答表之外，还需要去累加一下问答数或者评论数，如果是回答，那么需要更新问题表的回答数，如果是评论，需要更新回答表的评论数。如果是学员的评论，还需要修改问题表中的一个是否查看的字段，主要是将来方便管理员查看自己是否处理过这些问题。接着我们会发送一个MQ的消息，一般一次回答是给5个积分，推送给积分服务。

再说一下查询回答列表吧。列表中首先会查询问题的详情，在问题详情下就会有很多的回答跟评论。这里前端会给后端传递问题id或者回答id。我们直接分页查询回答表，查到数据之后还需要处理提问者的信息，回复目标的信息以及当前用户点赞状态等。提问者信息和回复者信息如果不是匿名的，我们就会通过远程调用去查询用户服务，并用Stream流转换成Map结构。另外还需要查询用户点赞状态，这个也需要调用点赞的微服务，判断用户是否点过赞。最后把这些信息封装VO返回。

**后续优化方案**

后续优化，把数据迁移到MongoDB中，并且相应的代码也会改成MongoDB的操作。

---

#### 3. 点赞功能

**业务介绍**

在天机学堂中涉及点赞的地方还是比较多的，用户可以给问答评论做点赞，可以给笔记做点赞等。所以点赞要做成一个通用的功能，我们是单独搭建了一个点赞的服务，具有独立性和通用性，并且能适应一定的并发。

**实现流程**

用户提交点赞信息后，会携带要点赞的业务id、点赞的业务类型到点赞服务。点赞服务主要保存点赞记录和点赞数量。

这里我们使用的Redis中的**Set结构**来保存点赞记录，key是业务的id，值就是点赞的用户。因为Set结构中的值不能重复，所以我们通过返回值就能判断该用户之前是否已经点过赞，如果已经点过，直接结束。如果没有点过赞，我们还会通过Set中**SCARD命令**来统计这个业务点赞的用户数量，再把点赞的数量保存到**Zset结构**中，key是业务类型，score是点赞数量，value是业务id。

在点赞服务中我们还开启了一个定时任务，这里使用的是**XXL-JOB**，定时从Zset结构中利用**popmin**从小到大批量的读取并移除这些业务的点赞数量，再通过**MQ通知业务方**，业务方会把点赞数量记录到自己的业务表中。

另外还提供Feign接口，供业务方根据业务id集合进行查询，可以判断当前登陆用户是否对这些业务点赞过。这里我们使用了**Pipeline**批量读取查询到的。

**可能追加提问**

- 为什么利用Zset结构来存储？Zset底层原理是什么？（跳表）

---

#### 4. 签到积分功能

**业务介绍**

用户在个人中心我的积分页面可以进行每日签到打卡，签完到之后会产生积分，我们规定了签一次到给一个积分，如果连续签到还会有额外的积分奖励，积分可以用来兑换优惠券、书籍周边等。积分除了通过点赞获得之外，还可以通过用户的学习行为获取，比如：学习、写笔记、回答问题等。这里我主要来说下签到的实现。签到需要记录用户的id、签到时间，而且签到时间将来还需要根据月进行统计。

一条记录是一个用户一次的签到记录。假如一个用户1年签到100次，而网站有100万用户，就会产生1亿条记录。随着用户量增多、时间的推移，这张表中的数据只会越来越多，占用的空间也会越来越大。

**实现流程**

我们使用**Redis BitMap数据结构**实现的。用户在提交点赞记录后我们首先会通过**SetBit指令**来保存用户的签到记录，key是用户id+年月，value就是BitMap了。其中每一位代表这个月的每一天的签到情况，0代表未签到，1代表已签到。offset偏移量就是今天的日期。这样我们用一行数据就能标示这个用户一个月的签到记录，最多31个比特位，不超过4个字节，非常的节省空间。

然后还要计算下连续签到天数，通过**bitField指令**查询出这个用户这个月从第一天到今天为止所有的签到记录，因为查到的是10进制，所以我们让这个数字跟1做与运算，其实就是最后一位跟1做与运算，这样就能得到最后一天的签到记录，如果是1，代表已经签到过，再把这个数字右移一位，倒数第二位就成最后一位了，再跟1做与运算，得到倒数第二天的签到记录，依次循环，直到碰到结果为0的情况，最终就能得到连续签到的天数了。

根据连续签到的天数的奖励积分，加上本次签到得分，就能知道用户本次签到的总积分了，通过MQ发送消息给积分系统，积分系统会保存签到积分。

---

#### 5. 积分排行榜功能

**业务介绍**

在个人中心我的积分页面可以查看到用户的积分获取情况，还能看到学霸天梯榜，也就是积分排行榜。分为实时排行榜和历史排行榜：
- 实时排行榜展示的是本月的积分获取情况，根据用户获取的积分倒序排名，只展示前10名，当前用户的排名始终都会展示。
- 历史排行榜跟实时排行榜类似，只不过是可以查询历史赛季（历史的月份）的排名情况。

**实现流程**

当用户通过点赞、学习行为产生积分时，会发送消息通知积分服务，积分服务除了保存积分明细之外，还会把积分累加到**Zset**中，Zset就记录的是用户本月的总积分。这个Zset就是实时榜单了。

用户在查询实时榜单时需要查询榜单列表和自己的排名：
- 查询榜单列表通过**reverseRangeWithScores**命令进行查询
- 查询用户自己的排名则通过**reverseRank**命令进行查询

历史榜单稍微会麻烦那么一点。因为历史榜单查询频率不高，所以我们没有在Redis中存储，是持久化到MySQL数据库中的。这里我们采用的是**水平分表**来存储，一个季度就是一张表。

通过**XXL-Job定时任务**来实现整个历史榜单持久化动作，分为三个定时任务：
1. 第一个定时任务是月初的时候创建新的赛季表
2. 第二个定时任务是在第一个定时任务执行完之后接着执行的，用来把上个月的榜单持久化到数据库中
3. 第三个定时任务是在第二个定时任务执行完之后接着执行，用来清理Redis中上个月的榜单数据

我主要说下第二个定时任务持久化的实现流程：首先获取到上个月的日期，然后从Redis中查询上个月的积分排名信息，再利用**MybatisPlus的动态表名插件**来计算对应的表，再把积分排行榜存储到数据库中，并且这里定时任务采用的是**XXL-JOB的任务分片**，每个实例按照自己的分片序号查询不同的数据进行持久化。

第三个定时任务我们使用了redis中的**unlink指令**异步删除redis中的榜单数据，防止阻塞。

---

#### 6. 优惠券管理功能/发放优惠券

**业务介绍**

优惠券管理是属于后台管理营销中心的功能。主要就是管理员对优惠券的创建、发放、查询、删除等操作。优惠券可以指定使用的范围，比如全部课程，或者部分分类的课程。还分为很多类型：每满减、满减、无门槛、折扣等。另外还能指定发放的数量、每人限领的数量。

跟发放优惠券相关的还有一个重要的属性就是推广的方式，分为**手动领取**和**指定发放**：
- 如果是手动领取，用户直接在活动页面点击领取就行
- 如果是指定发放，那么就需要生成兑换码，用户在兑换码页面输入兑换码来兑换优惠券，一个兑换码只能使用一次

**实现流程**

管理员刚创建的优惠券其实是处于待发放状态，确认没有问题了，管理员就能发放优惠券了。在发放时，可以指定立即发放或者定时发放。

在后台，首先会查询优惠券，再判断优惠券的状态是处于待发放或者暂停，这两种状态下才可以进行发放。如果是立即发放，那么直接修改数据库优惠券状态即可。如果推广方式是手动领取，流程到此结束。但如果是指定发放，我们还需要生成兑换码。需要支持20亿以上的唯一兑换码，长度不超过10，只能包含字母数字，并且要保证生成和校验算法的高效。

这里我们使用了**自增序列**来实现的。因为自增序列号可以借助于**BitMap**验证兑换状态，完全不用查询数据库，效率非常高。要满足20亿的兑换码需求，只需要31个bit位就够了，也就是在Integer的取值范围内，非常节省空间。我们就按32位来算，支持42亿数据规模。

不过，仅仅使用自增序列还不够，因为容易被人猜到爆刷。所以我们设计了一个**按位加权求和的加密验签算法**：
- 32位的自增序列，可以每4位一组，转为10进制，这样就有8个数字
- 提前准备一个长度为8的**加权数组**，作为秘钥。对自增序列的8个数字按位加权求和，得到的结果作为签名。加权数组最好使用质数，这样重复的概率会降低
- 考虑到秘钥的安全性，我们也可以准备多组加权数组，比如准备16组。然后生成兑换码时随机生成一个4位的新鲜值，取值范围刚好是0~15，新鲜值是几，我们就取第几组加权数组作为秘钥
- 然后把新鲜值、自增序列拼接后按位加权求和，得到签名
- 最后把签名值的后14位、新鲜值（4位）、自增序列（32位）拼接，得到一个50位二进制数，然后与一个较大的质数做**异或运算**加以混淆，再基于**Base32**进行转码，得到的兑换码恰好10位，符合要求

当我们要验签的时候：
1. 首先将结果利用Base32转码为数字，然后与大质数异或得到原始数值
2. 接着取高14位，得到签名；取后36位得到新鲜值与自增序列的拼接结果；取中4位得到新鲜值
3. 根据新鲜值找到对应的秘钥（加权数组），然后再次对后36位加权求和，得到签名。与高14位的签名比较是否一致，如果不一致证明兑换码被篡改过，属于无效兑换码。如果一致，证明是有效兑换码
4. 接着，取出低32位，得到兑换码的自增序列号。利用**BitMap**验证兑换状态，是否兑换过即可

整个验证过程完全不用访问数据库，效率非常高。

---

#### 7. 优惠券的领取功能

**业务介绍**

在营销活动页面，会展示出所有可以手动领取的、发放中的优惠券，学员用户可以在该页面领取优惠券。至于兑换码需要用户手动输入兑换码进行领取。每个优惠券都有库存数量和用户限领数量，所以我们在领取的时候需要判断是否超过库存、是否超出限领数量。用户领取成功，这个优惠券就是用户券，是单独存放在一张表中，用来记录用户和券的关系。

**实现流程**

当用户点击领取时，携带优惠券id请求到达后台。这里流程还是比较复杂的，我主要说下核心流程：

首先要判断库存，在优惠券表中有一个字段用来记录已领取的数量。接着还要判断限领数量，限领数量也在优惠券表中，判断的时候我们还会查询用户券，判断用户领取的这个券是否超过限领数量。都没有问题了，就会修改优惠券表中的已领取数量+1，同时生成一张用户券，其实就是给用户券表插入一条数据。

至于通过兑换码领取也类似，只不过多了几个流程：兑换码领取刚进来是需要校验并解析验证码（上面讲过），接下来的流程跟手动领取基本一样。

这就是领取优惠券的整个流程，不过这里会有一些**并发安全**的问题产生：

**第一个就是超卖问题**：原因就是判断库存数量和修改库存数量不是原子性操作。这种场景非常适合使用**乐观锁**来解决，在修改的时候加入库存数量的判断即可。

**第二个是超出用户限领数量问题**：原因也是因为这里的逻辑分三步：先查询数据库 --> 再判断是否超出限领数量 --> 最后新增用户券。解决方式的话这里不适合使用乐观锁了，因为是新增操作，所以只能使用**悲观锁**了。这里我们基于**Redisson**结合工厂模式（用来切换锁类型）和策略模式（锁失败之后的各种策略），还有AOP和自定义组件实现了一套**通用的锁组件**。只需要在对应的方法上加上注解就能实现悲观锁了。这里锁的粒度是用户id，所以性能还是比较ok的。

但是这里我们加了锁之后发现还是出现**锁失效**的问题：通过各种测试与排查，我们发现是**事务的原因**。整体业务流程是：开启事务 → 获取锁 → 统计用户已领券的数量 → 判断是否超出限领数量 → 如果没超，新增一条用户券 → 释放锁 → 提交事务。由于锁过早释放，导致了事务尚未提交，那么另一个线程过来可能查询不到未提交的数据，也认为当前用户没有领过券，最后也会导致出现一个用户领了多张券的情况。

**解决方案**就是调整事务的边界：业务开始前，先获取锁，再开启事务。业务结束后：先提交事务，再释放锁。所以我们把这部分核心代码抽取到一个方法中加了事务，锁的话我们利用Spring的**Ordered注解**把他的优先级调高了。

这样做并发安全问题能解决了。但是**事务又失效了**：原因是非事务方法调用了事务方法。后来我们利用**AspectJ**在非事务方法中获取到当前Bean的代理对象，利用代理对象调用方法就能使事务生效了。

**可能追加提问**

- 悲观锁和乐观锁的区别？分别有哪些实现方案？
- 为什么使用Redisson？（说一下集群场景下Synchronized的问题）
- Redisson是如何解决原子性问题？锁超时问题？可重入问题？失败重试问题？一致性问题？
- 你们通用锁组件是怎么实现的？具体讲讲
- 都有哪些锁类型？
- 都有哪些失败重试的策略？

---

#### 8. 通用分布式锁封装

**实现流程**

我们分布式锁使用的是Redisson，在项目中我们有很多地方都会使用到分布式锁，比如优惠券领取，兑换码兑换等场景，所以我们基于**AOP + 自定义注解**结合**工厂模式策略模式**实现了一套自定义的分布式锁。

首先分布式锁的使用无非就是在执行业务之前获取到锁对象，尝试获取锁，在业务执行完毕后去释放锁。所以我们写了一个**自定义的注解**，将来在需要加锁的方法上加上这个注解就行。另外还写了一个**AOP切面**，切入点就是加了注解的方法，通知类型是环绕通知。

在切面中主要做的事就是：获取锁对象 → 尝试加锁 → 执行业务 → 释放锁

- **首先获取锁对象**，我们使用**工厂模式**来实现。因为Redisson中有很多锁对象，比如可重入锁、公平锁、读锁、写锁等，我们把这些类型放到一个枚举中，并用枚举的方法来实现，然后配置到注解的属性上。将来使用的时候只需要在添加注解的时候去指定要用的锁类型就行
- **接着是尝试加锁**，这里我们使用的是**策略模式**实现的。主要是封装获取锁失败后的策略。比如获取失败后是否重试，是有限重试还是无限重试，如果不重试或者重试耗尽后又怎么处理，是直接结束还是抛出异常。我们把这些封装成一个一个的策略，目前是五种策略：不重试直接结束、不重试直接抛出异常、重试超时后结束、重试超时后抛出异常、无限重试。也是使用的枚举实现，并配置到注解中，在使用时只需要在注解中指定一种即可
- 最后获取锁成功后就开始执行业务。业务执行完成后释放锁

---

#### 9. 优惠券的使用功能

**业务介绍**

用户在领取成功优惠券之后，可以添加课程到购物车，下单的时候就可以使用优惠券了。首先在订单确认（预下单）时会查询出所有可用的优惠券组合，因为一个用户可能有多张券同时使用，并且每个券能应用的课程也不一样。在下单的时候需要查询优惠明细，并核销优惠券。取消订单的时候还需要返还优惠券。在查询订单时也需要查询优惠的规则。

我这里主要做的就是在预下单时查询可用的优惠券这个功能。用户在预下单时调用的是订单微服务，订单微服务会携带用户购买的多个课程调用优惠券的微服务，在优惠券的微服务中就需要筛选出可用的优惠券组合。

**实现流程**

首先会查询这个用户所有的可用优惠券。接着进行**初步筛选**，这里会先把金额不达标的给剔除，比如课程总金额少于使用门槛的，可以直接剔除掉了。

接着是**细筛**，然后**全排列**。找出每一个优惠券的可用的课程，判断课程总价是否达到优惠券的使用需求，然后再把这些可用的券排列组合。因为多张优惠券使用顺序不同，优惠的金额也不同，所以我们排列出所有的组合。

接下来**计算每种组合的优惠明细**，这里我们通过**CompletableFuture多线程**的方式进行并行计算，一种组合一个线程。同时利用**CountDownLatch**定义闭锁。等所有线程执行完后最后还需要**筛选出最优的方案**。遍历每种方案，判断用券相同时，那种优惠券金额最大。判断优惠券金额相同时，那种方案用券最少，取交集。以此计算出最优解。

**可能追加提问**

- 折扣类型有哪些？你们是怎么计算每种折扣类型的？（每满减、折扣、无门槛、满减，每种折扣类型对应一种策略，使用策略模式实现）
- CompletableFuture原理是什么？
- CountDownLatch原理是什么？

---

## 二、黑马点评项目

### 项目介绍

黑马点评是一个基于SpringBoot开发的前后端分离项目，类似于大众点评，是一个高并发本地生活服务平台。实现了短信登录、商户查询缓存、优惠券秒杀、附近的商户、UV统计、用户签到、好友关注、达人探店等模块，形成了业务闭环。项目中重点使用了Redis解决高并发场景下的缓存、秒杀、分布式锁等问题，并结合RabbitMQ实现异步下单。

### 技术栈

SpringBoot、MyBatis-Plus、MySQL、Redis、Redisson、RabbitMQ、Lombok、Hutool、JWT、Nginx、Maven

### 项目负责（核心功能）

1. 负责实现基于Redis的短信登录功能，使用Token + 双拦截器方案解决集群模式下的Session共享问题，实现登录状态的自动续约。
2. 负责商户信息缓存模块，采用Cache Aside模式解决数据库与缓存的一致性问题，通过返回空值避免缓存穿透，设置随机过期时间防止缓存雪崩，使用互斥锁解决缓存击穿问题。
3. 负责优惠券秒杀模块，使用Redis + Lua脚本实现对用户秒杀资格的预检，利用Redis分布式锁解决集群模式下一人一单的线程安全问题，基于RabbitMQ实现异步秒杀下单，提升系统吞吐量。
4. 负责点赞排行榜功能，使用Redis的ZSet数据结构实现，支持按时间排序的点赞排行榜查询。
5. 负责好友关注与共同关注功能，使用Redis的Set集合实现关注关系存储，利用SINTER命令实现共同关注查询。
6. 负责附近商户查询功能，使用Redis的GEO数据结构实现基于地理位置的附近商家搜索。
7. 负责用户签到功能，使用Redis的BitMap实现高性能、低存储成本的签到记录，支持连续签到天数统计。
8. 负责UV统计功能，使用Redis的HyperLogLog实现海量数据的UV统计，极大降低内存占用。

### 项目表述

#### 1. 短信登录模块

**业务介绍**

用户通过手机号获取验证码进行登录，登录成功后可以浏览商户、参与秒杀、发布探店笔记等。由于项目是前后端分离且部署在集群环境下，传统的Session方案无法满足需求。

**实现流程**

我们使用**Redis + Token**方案来解决集群模式下的Session共享问题：
1. 用户输入手机号，后端生成验证码并存储到Redis中，设置过期时间为2分钟
2. 用户提交手机号和验证码，后端校验通过后，生成一个全局唯一的token作为用户登录凭证
3. 将用户信息以Hash结构存储到Redis中，key为登录用户前缀 + token，并设置30分钟的过期时间
4. 将token返回给前端，前端存储在localStorage中，后续每次请求都携带token
5. 后端使用**双层拦截器**方案：第一层拦截器负责刷新token有效期（每次访问都刷新），第二层拦截器负责校验用户是否登录
6. 当token有效期不足10分钟时，自动续约，保证用户登录状态的连续性

**可能追加提问**

- 为什么不用JWT？
- 两层拦截器分别的作用是什么？
- 登录状态是如何续约的？

---

#### 2. 商户缓存模块

**业务介绍**

商户信息是用户访问频率最高的数据之一，为了降低数据库压力，我们需要对商户信息进行缓存。但引入缓存后又会带来缓存与数据库数据不一致的问题，以及高并发下的缓存穿透、雪崩、击穿等问题。

**实现流程**

我们采用**Cache Aside**缓存更新策略，通过主动更新 + 超时剔除来保证数据的一致性：
- **缓存穿透**：对于查询不到的数据，我们也缓存一个空值（设置较短的过期时间，如2分钟），防止恶意攻击不断查询不存在的数据
- **缓存雪崩**：为不同的缓存设置不同的过期时间（在基础时间上增加一个随机值），防止大量缓存同时失效导致数据库压力骤增
- **缓存击穿**：对于热点数据，使用**互斥锁**（基于Redis的setnx实现）来保证同一时间只有一个线程去查询数据库并重建缓存，其他线程等待缓存重建完成后直接从缓存获取

**可能追加提问**

- 缓存穿透、雪崩、击穿的区别？分别有哪些解决方案？
- Cache Aside模式的具体流程？
- 如何保证数据库与缓存的一致性？

---

#### 3. 优惠券秒杀模块

**业务介绍**

优惠券秒杀是高并发场景下的典型业务，需要解决超卖问题、一人一单问题，同时要保证系统的高可用和高性能。

**实现流程**

我们的秒杀方案经历了三次演进：

**方案一：数据库悲观锁（SELECT ... FOR UPDATE）**
- 问题：锁竞争激烈，性能差，大量请求直接打到数据库

**方案二：数据库乐观锁（UPDATE SET stock = stock - 1 WHERE stock > 0）**
- 问题：虽然解决了超卖，但仍有大量请求打到数据库，数据库压力依然很大

**方案三：Redis + Lua脚本（本项目最终方案）**
- 在Redis层面完成库存校验和扣减，不打数据库
- 使用**Lua脚本**保证库存判断和扣减的原子性
- 使用**Redis的Set结构**记录已下单用户，实现一人一单
- 秒杀资格预检通过后，发送消息到**RabbitMQ**，实现**异步下单**，降低系统响应时间
- 消费者从MQ中获取消息，完成最终的订单创建和数据库扣减

**Lua脚本核心逻辑**：
1. 判断库存是否充足
2. 判断用户是否已经下过单（一人一单）
3. 扣减库存 + 记录用户已下单
4. 返回成功标识

**可能追加提问**

- 超卖问题有哪些解决方案？各有什么优缺点？
- 为什么用Lua脚本？Lua脚本如何保证原子性？
- 异步下单有什么好处？RabbitMQ在这里的作用？
- 分布式锁有哪些实现方式？Redisson的原理？

---

#### 4. 点赞排行榜

**业务介绍**

用户可以对探店笔记进行点赞，并且需要展示点赞排行榜（按点赞时间排序）。

**实现流程**

使用Redis的**ZSet（Sorted Set）**数据结构实现：
- key为笔记id，value为点赞的用户id，score为点赞的时间戳
- 点赞时：使用**ZADD**命令将用户id添加到对应笔记的ZSet中，score为当前时间戳
- 取消点赞时：使用**ZREM**命令将用户id从ZSet中移除
- 查询点赞排行榜时：使用**ZREVRANGE**命令按score（时间戳）倒序查询前N名
- 查询用户是否点赞：使用**ZSCORE**命令判断用户id是否在ZSet中

**可能追加提问**

- 为什么用ZSet而不是Set？
- ZSet的底层实现是什么？（跳表 + 哈希表）
- 如果要按点赞数量排序怎么做？

---

#### 5. 好友关注与共同关注

**业务介绍**

用户可以关注其他用户（达人），查看自己关注的人和粉丝，还可以查看与好友的共同关注。

**实现流程**

使用Redis的**Set**数据结构实现：
- 关注集合：key为"follows:{用户id}"，value为被关注的用户id集合
- 粉丝集合：key为"fans:{用户id}"，value为关注该用户的用户id集合
- 关注操作：使用**SADD**命令同时维护关注集合和粉丝集合
- 取消关注：使用**SREM**命令同时从两个集合中移除
- 共同关注：使用**SINTER**命令求两个关注集合的交集

**可能追加提问**

- SINTER命令的复杂度？
- 如果数据量很大，Set的交集操作会不会很慢？
- 除了Redis，还可以用什么方案实现共同关注？

---

#### 6. 附近商户查询

**业务介绍**

用户可以根据当前位置查询附近的商户，并按距离排序。

**实现流程**

使用Redis的**GEO**数据结构实现：
- 使用**GEOADD**命令将商户的经纬度信息添加到Redis中，key为商户类型，member为商户id
- 查询时使用**GEORADIUS**或**GEOSEARCH**命令，以用户当前位置为圆心，指定半径范围查询附近的商户
- 使用**WITHDIST**参数返回距离，实现按距离排序
- Redis GEO底层是基于**ZSet**实现的，使用**Geohash**算法将经纬度编码为字符串

**可能追加提问**

- Geohash的原理是什么？
- GEO的精度问题怎么解决？
- 如果商户数量非常大，怎么优化？

---

#### 7. 用户签到功能

**业务介绍**

用户每天可以进行签到，连续签到有额外奖励。需要记录用户每天的签到情况，并支持快速统计连续签到天数。

**实现流程**

使用Redis的**BitMap**数据结构实现：
- key为"sign:{用户id}:{年月}"，如"sign:1001:202401"
- 使用**SETBIT**命令记录签到，offset为当天的日期（1-31），value为1表示签到
- 使用**GETBIT**命令查询某天是否签到
- 使用**BITFIELD**命令一次性获取当月所有签到数据
- 统计连续签到天数：从最后一天开始，逐位向前检查，直到遇到未签到的位为止
- 存储成本极低：一个用户一个月的签到记录只占31个bit，不到4个字节

**可能追加提问**

- BitMap的原理？为什么适合签到场景？
- 一年的签到数据占多大空间？
- 除了BitMap，还有什么方案？

---

#### 8. UV统计功能

**业务介绍**

需要统计网站的独立访客数（UV），数据量可能非常大（百万级甚至亿级），但允许一定的误差。

**实现流程**

使用Redis的**HyperLogLog**数据结构实现：
- 使用**PFADD**命令将用户id添加到HyperLogLog中
- 使用**PFCOUNT**命令统计独立访客数
- HyperLogLog只占用**12KB**的内存，无论数据量多大
- 误差率约为**0.81%**，对于UV统计场景完全可接受
- 还可以使用**PFMERGE**命令合并多个HyperLogLog，实现多维度统计

**可能追加提问**

- HyperLogLog的原理？（概率算法，基于调和平均数）
- 误差率多少？为什么只占12KB？
- 什么场景下不适合用HyperLogLog？

---

## 三、启山智软 SmartShop 项目

### 项目介绍

启山智软 SmartShop 是一款经过百万真实用户检验的电商业务中台系统，采用前后端分离、分布式微服务架构。系统通过订单中心、会员中心、营销中心、商家中心、商品中心、财务中心等模块进行管理，支持多种业务属性的共性能力，实现了快速部署和服务复用。

系统支持多种商业模式：S2B2C供应链商城、B2B2C多商户商城、O2O外卖商城、B2C单商户商城、社区团购、B2B批发商城等。含有秒杀、直播、优惠券、满减、砍价、分销、套餐、拼团、消费返利、平台抽佣、储值、同城配送、到店自提等营销功能。

### 技术栈

JDK17/21、Spring Boot 2.7+、Spring Cloud Alibaba、Spring Cloud Gateway、Nacos、Sentinel、Dubbo 3、MyBatis-Plus、Redis、Redisson、RabbitMQ、Elasticsearch、XXL-Job、ShardingJDBC、Dynamic-Datasource、Canal、SkyWalking、OSS、Vue3、UniApp、TypeScript、Element Plus、Pinia、Vite

### 项目负责（核心功能）

1. 负责商品中心模块，实现商品SPU/SKU管理、商品上下架、库存管理，使用Redis缓存热点商品数据，基于Canal实现数据库与缓存的数据同步。
2. 负责订单中心核心流程，实现下单、支付、发货、收货、退款等状态机流转，使用Redisson分布式锁解决库存扣减的并发安全问题，基于RabbitMQ实现订单超时自动取消。
3. 负责秒杀活动模块，使用Redis + Lua脚本实现库存预扣减，基于Redisson分布式锁实现一人一单，通过RabbitMQ异步处理秒杀订单，实现流量削峰。
4. 负责优惠券系统，支持满减、折扣、无门槛等多种优惠券类型，实现优惠券的创建、发放、领取、使用、核销全生命周期管理。
5. 负责会员中心积分模块，使用Redis ZSet实现积分实时排行榜，基于XXL-Job定时任务实现历史排行榜的持久化和赛季切换。
6. 负责商家入驻与店铺管理模块，实现商家资质审核、店铺装修、商品管理、订单管理、财务结算等功能。
7. 负责支付中心对接，集成微信支付、支付宝等第三方支付，实现支付回调、退款、对账等功能，基于MQ保证支付状态的最终一致性。
8. 负责系统性能优化，使用ShardingJDBC实现读写分离和分库分表，基于SkyWalking实现全链路性能监控和慢查询定位。

### 项目表述

#### 1. 商品中心与库存管理

**业务介绍**

商品中心是电商系统的核心模块，需要管理商品的SPU（标准产品单元）和SKU（库存量单位），支持商品的上架、下架、价格调整、库存管理等操作。商品数据访问频率极高，需要合理的缓存策略。

**实现流程**

我们采用**SPU + SKU**的两层商品模型：
- **SPU**：描述商品的基本信息（名称、品牌、分类、图片、详情等）
- **SKU**：描述商品的具体销售单元（规格组合、价格、库存、条码等）

缓存策略：
- 使用**Redis**缓存热点商品数据，key为"product:{skuId}"
- 使用**Canal**监听MySQL的binlog，当商品数据发生变化时，异步更新Redis缓存，保证数据库与缓存的最终一致性
- 对于商品详情页，采用**多级缓存**架构：本地缓存（Caffeine）→ Redis缓存 → 数据库
- 库存数据使用**Redis**进行预扣减，定期同步到数据库

**可能追加提问**

- SPU和SKU的区别？
- Canal的原理是什么？如何保证数据一致性？
- 多级缓存的优缺点？

---

#### 2. 订单中心与状态机

**业务介绍**

订单中心负责处理用户的下单流程，包括购物车确认、地址选择、优惠券选择、支付、发货、收货、评价、退款等完整的订单生命周期。订单状态流转复杂，需要保证数据的一致性和可靠性。

**实现流程**

我们采用**状态机模式**管理订单状态流转：
- 订单状态：待付款 → 待发货 → 待收货 → 已完成 → 已取消
- 退款状态：退款申请 → 退款审核 → 退款中 → 已退款/已拒绝

核心流程：
1. 用户提交订单，后端进行参数校验、库存校验、优惠券校验
2. 使用**Redisson分布式锁**（锁粒度：skuId）保证库存扣减的并发安全
3. 订单创建成功后，发送延迟消息到**RabbitMQ**（TTL + 死信队列），30分钟后如果订单未支付，自动取消订单并回滚库存
4. 用户支付成功后，支付服务发送MQ消息，订单服务消费消息后更新订单状态为"待发货"
5. 发货、收货等环节都是通过状态机进行流转，每个状态变更都会记录订单操作日志

**可能追加提问**

- 状态机模式的优势？
- 分布式锁的粒度如何选择？
- 订单超时取消怎么实现？
- 如何保证支付和订单状态的一致性？

---

#### 3. 秒杀活动模块

**业务介绍**

秒杀是电商平台常用的营销手段，特点是大量用户在短时间内抢购少量商品，并发量极高，需要解决超卖、重复购买、系统压力等问题。

**实现流程**

秒杀系统采用分层过滤、逐级削峰的架构：

**第一层：前端限流**
- 按钮置灰、验证码、随机等待，减少无效请求

**第二层：Nginx限流**
- 使用Nginx的limit_req模块进行IP级别的限流

**第三层：Gateway限流**
- 使用Sentinel进行QPS限流、热点参数限流

**第四层：Redis预扣减（核心）**
- 使用**Lua脚本**在Redis中完成库存校验和扣减，保证原子性
- 使用**Set结构**记录已购买用户，实现一人一单
- Lua脚本返回成功/失败，失败的直接返回，不进入后续流程

**第五层：MQ异步下单**
- 预扣减成功的请求，发送消息到**RabbitMQ**
- 消费者异步创建订单、扣减数据库库存
- 如果异步下单失败，通过补偿机制回滚Redis库存

**第六层：数据库**
- 最终只有少量请求会到达数据库，完成真正的订单创建

**可能追加提问**

- 秒杀系统的架构设计？
- 如何保证不超卖？
- 如果Redis预扣减成功了，但后续下单失败了怎么办？（补偿机制）
- 如何应对恶意刷单？

---

#### 4. 优惠券系统

**业务介绍**

优惠券系统是营销中心的重要组成部分，需要支持多种优惠券类型（满减、折扣、无门槛等），以及多种发放方式（手动领取、兑换码、定向发放等）。优惠券的领取和使用涉及库存管理和并发控制。

**实现流程**

**优惠券创建**：
- 后台配置优惠券的基本信息（类型、面额、门槛、有效期、发放总量、每人限领数量等）

**优惠券发放**：
- 手动领取：用户在前端点击领取
- 兑换码：生成兑换码，用户输入兑换码领取
- 定向发放：系统主动推送给指定用户

**优惠券领取**：
- 使用**乐观锁**解决超发问题：UPDATE coupon SET issue_num = issue_num + 1 WHERE issue_num < total_num
- 使用**Redisson分布式锁**解决用户重复领取问题
- 领取成功后，在user_coupon表中生成用户券记录

**优惠券使用**：
- 在下单时，查询用户可用的优惠券列表
- 根据优惠券规则（门槛、有效期、适用商品范围）筛选可用券
- 计算优惠金额，核销优惠券
- 订单取消时，返还优惠券

**可能追加提问**

- 优惠券超发怎么解决？
- 乐观锁和悲观锁的选择？
- 优惠券的核销是下单时扣减还是支付时扣减？

---

#### 5. 积分排行榜

**业务介绍**

为了促进用户活跃度，系统设置了积分体系。用户通过购物、签到、评价等行为获取积分，积分可以在积分商城兑换商品或抵扣现金。同时有积分排行榜功能，展示积分最高的用户。

**实现流程**

**积分获取**：
- 用户行为埋点，发送MQ消息到积分服务
- 积分服务消费消息，计算积分并保存到积分记录表
- 同时累加到Redis的**ZSet**中，key为"points_board:{年月}"

**实时排行榜**：
- 使用Redis **ZSet**存储，member为用户id，score为积分
- 查询榜单：使用**ZREVRANGE**命令倒序查询前N名
- 查询个人排名：使用**ZREVRANK**命令

**历史排行榜**：
- 每月1号凌晨，使用**XXL-Job**定时任务将上月的Redis数据持久化到MySQL
- MySQL按月份**水平分表**，表名为"points_board_202401"
- 使用**ShardingJDBC**实现分表路由
- 持久化完成后，使用**UNLINK**异步删除Redis中的上月数据

**可能追加提问**

- ZSet为什么适合做排行榜？
- 分库分表的策略？
- XXL-Job的任务分片怎么用？

---

#### 6. 商家入驻与店铺管理

**业务介绍**

平台支持多商户入驻，商家可以申请开店，上传资质，审核通过后可以管理自己的商品、订单、财务等。

**实现流程**

**商家入驻流程**：
1. 商家提交入驻申请，填写企业信息、联系人、经营范围等
2. 上传营业执照、法人身份证等资质文件（存储到OSS）
3. 平台运营人员审核（后台管理系统）
4. 审核通过后，创建商家账号和店铺，分配店铺ID

**店铺管理**：
- 商家后台：使用**Vue3 + Element Plus**构建，支持店铺装修（拖拽式DIY）
- 商品管理：商家可以发布、编辑、上下架自己的商品
- 订单管理：处理用户的订单，进行发货、退款等操作
- 财务结算：平台定期与商家进行结算，扣除平台佣金

**权限控制**：
- 使用**Spring Security OAuth2**实现权限控制
- 商家只能操作自己店铺的数据，通过**数据权限拦截器**实现行级数据隔离

**可能追加提问**

- 多租户的数据隔离方案？
- 商家和平台的数据一致性怎么保证？
- 店铺装修是怎么实现的？

---

#### 7. 支付中心

**业务介绍**

支付中心负责对接多种第三方支付渠道（微信支付、支付宝等），处理用户的支付请求、支付回调、退款、对账等。

**实现流程**

**支付流程**：
1. 用户选择支付方式，调用支付中心的统一下单接口
2. 支付中心根据支付方式调用对应的第三方支付接口（如微信JSAPI支付）
3. 获取支付参数，返回给前端，前端调起支付SDK
4. 用户完成支付后，第三方支付平台异步通知支付中心（回调接口）
5. 支付中心校验签名，更新订单支付状态，发送MQ消息通知订单服务
6. 订单服务消费消息，更新订单状态为"待发货"

**可靠性保证**：
- 支付回调可能丢失或延迟，所以我们同时启动**定时任务**（每5分钟）查询未支付的订单，主动调用第三方平台的订单查询接口
- 支付记录和订单状态通过**MQ**保证最终一致性
- 对于退款，先创建退款记录，状态为"退款中"，退款成功后再更新状态

**对账**：
- 每天凌晨下载第三方支付平台的账单
- 与系统内的支付记录进行比对，找出差异记录
- 差异记录人工处理

**可能追加提问**

- 支付回调丢失怎么办？
- 如何保证支付状态的一致性？
- 对接微信支付和支付宝的区别？

---

#### 8. 系统性能优化

**业务介绍**

随着业务发展，系统的数据量和访问量不断增长，需要进行系统性的性能优化，包括数据库优化、缓存优化、异步化改造等。

**实现流程**

**数据库优化**：
- 使用**ShardingJDBC**实现**读写分离**：写操作走主库，读操作走从库
- 对于订单表、积分记录表等大数据量表，进行**水平分表**，按时间或哈希取模分片
- 合理设计索引，定期进行慢查询分析

**缓存优化**：
- 热点数据（商品信息、用户信息）缓存到Redis
- 本地缓存（Caffeine）+ Redis分布式缓存的两级缓存架构
- 使用**Canal**实现数据库与缓存的同步

**异步化改造**：
- 非核心流程异步化：发送短信、推送通知、积分计算等通过MQ异步处理
- 订单超时取消使用RabbitMQ延迟队列

**监控**：
- 使用**SkyWalking**进行全链路追踪，定位性能瓶颈
- 使用**Sentinel**进行熔断降级，保护系统稳定性
- 使用**Prometheus + Grafana**进行系统指标监控

**可能追加提问**

- 读写分离的实现方式？主从延迟怎么解决？
- 分库分表后，跨分片的查询怎么解决？
- 全链路追踪的原理？
- 熔断降级的策略？

---

## 附录：面试通用问题

### 1. 项目是如何搭建/部署的？

天机学堂：我们项目中包含的服务有学习微服务、课程微服务、点赞微服务还有网关等十几个微服务。涉及到的中间件也很多，比如MySQL数据库、Redis、MQ、ES、注册中心Nacos等等。这里我们是使用Jenkins搭建的，当我们写完代码提交到自己的git私服上（Gogs），在Gogs上我们配置了web钩子，发现有提交后就会触发钩子通知Jenkins，Jenkins会自动执行拉取代码、构建项目，并自动部署成Docker容器，最后我们还配置了自动化测试脚本，可以自动测试。

黑马点评：项目使用SpringBoot开发，部署在Tomcat集群中，使用Nginx作为反向代理和负载均衡。Redis和MySQL也采用集群部署。

启山智软：基于Spring Cloud Alibaba微服务架构，使用Docker容器化部署，Nacos作为注册中心和配置中心，Gateway作为统一网关，各微服务独立部署，支持弹性伸缩。

### 2. 项目中成员组成？

参考：
- 1个项目经理
- 2个产品经理
- 7个后端开发
- 2个前端开发
- 1个测试人员
- 0个运维（大公司一般运维不跟随项目组）

### 3. 项目中登录认证流程？

天机学堂：用户登录成功之后后端会给前端返回一个token。接下来用户发起的每个请求都会携带token，每个请求到达后台都会先经过网关，在网关中我们配置了过滤器，用来获取token并解析，拿到用户id。接着会把用户id放到请求头中，转发给微服务。每个微服务都会有一个拦截器，在拦截器中会获取请求头中的用户id，并且把用户id存入ThreadLocal中。将来在业务代码中需要用到登录用户，直接从ThreadLocal中获取即可。

黑马点评：基于Redis + Token方案，双层拦截器实现登录校验和token续约。

---

> 📎 关联文档
> - [[天机学堂-MOC|课程索引]]
> - [[16-项目面试总结]]
> - [[17-项目表述]]
