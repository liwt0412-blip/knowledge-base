---
tags:
  - Milvus
  - Docker
  - 环境搭建
  - EduRAG
title: Milvus环境搭建（自己搭建）
description: Docker 部署 Milvus（etcd + minio + milvus）完整流程
date: 2025-06-22
sources:
  - 黑马课程讲义: EduRAG项目
---

# Milvus环境搭建（自己搭建）

## 1. 安装docker镜像

远程服务器上安装好docker及docker compose之后，上传资料中的镜像文件，然后进行加载到服务器中

![](images/media/image1.png)

```bash
docker load -i redis.tar\| docker load -i etcd.tar \| docker load -i minio.tar \| docker load -i milvusdb.tar

```

加载成功之后的效果：

![](images/media/image2.png)

## 2. Docker compose启动容器

创建项目目录

创建一个文件夹，用于存放 Milvus 和 Redis 的配置文件和数据：

例如：

```bash
mkdir /root/ai/dev/milvus_redis
cd /root/ai/dev/milvus_redis

```

创建 Docker Compose 文件

在 /root/ai/dev/milvus_redis 目录下创建文件：docker-compose.yml 内容如下：

```yaml
services:
etcd:
container_name: milvus-etcd
image: quay.io/coreos/etcd:v3.5.5
environment:
- ETCD_AUTO_COMPACTION_MODE=revision
- ETCD_AUTO_COMPACTION_RETENTION=1000
- ETCD_QUOTA_BACKEND_BYTES=4294967296
- ETCD_ENABLE_V2=true
volumes:
- ./volumes/etcd:/etcd/data
command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 \--data-dir /etcd/data

minio:
container_name: milvus-minio
image: minio/minio:RELEASE.2023-03-20T20-16-18Z
environment:
MINIO_ACCESS_KEY: minioadmin
MINIO_SECRET_KEY: minioadmin
volumes:
- ./volumes/minio:/minio/data
command: minio server /minio/data
healthcheck:
test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
interval: 30s
timeout: 20s
retries: 3

standalone:
container_name: milvus-standalone
\# 使用多架构镜像，Docker on Windows 会自动拉取 x86_64 版本
image: milvusdb/milvus:v2.4.4
command: ["milvus", "run", "standalone"]
environment:
- ETCD_ENDPOINTS=etcd:2379
- MINIO_ADDRESS=minio:9000
volumes:
- ./volumes/milvus:/var/lib/milvus
ports:
- "19530:19530"
- "9091:9091"
depends_on:
- "etcd"
- "minio"

redis:
container_name: milvus-redis
image: redis:latest
restart: always
ports:
- "6379:6379"
volumes:
- ./volumes/redis:/data
\# 设置 Redis 密码为 1234
command: redis-server \--requirepass 1234

networks:
default:
name: milvus-network

```

说明

Milvus 服务

image: 使用 Milvus 官方镜像 milvusdb/milvus:v2.4.10。

ports: 映射 gRPC（19530）和 RESTful API（9091）端口。

volumes: 数据持久化存储到本地 ./volumes/milvus 目录。

environment: 配置日志级别和 Etcd 参数。

command: 启动 Milvus 单机模式。

Redis 服务

image: 使用 Redis 官方镜像 redis:7.2。

ports: 映射默认端口 6379。

volumes: 数据持久化存储到本地 ./volumes/redis 目录。

command: 启用 AOF（Append Only File）持久化模式。

volumes: 定义持久化存储卷。

启动 Milvus 和 Redis

在 /root/ai/dev/milvus_redis 目录下，打开 PowerShell，运行命令：docker compose up -d

![](images/media/image3.png)

检查容器是否正常运行，执行命令：docker ps

停止服务命令：docker compose down

## 3. 验证Milvus 服务

验证milvus是否安装成功，可以使用可视化工具连接即可，在当天的资料中找到对应的软件

![](images/media/image4.png)

```
同样提供了两个版本，分别是Windwos和Mac系统对应的版本

```

安装成功之后，可以直接连接本地的Milvus服务

![](images/media/image5.png)

连接成功后，可以看到默认的数据库

![](images/media/image6.png)

如果以上操作都没问题，代表Milvus服务成功安装！

## 4. 验证Redis服务

可以使用Redis本地连接工具(Another Redis Desktop Manager)进行测试

![](images/media/image7.png)

连接成功后的效果：

![](images/media/image8.png)

常见问题：

## 5. 常见问题与解决方法

端口冲突

如果 19530、9091 或 6379 端口被占用，修改 docker-compose.yml 中的端口映射（例如 19531:19530），并在客户端连接时使用新端口。

镜像拉取失败

确保网络连接正常，或尝试手动拉取镜像：

```
docker pull milvusdb/milvus:v2.4.10
docker pull redis:7.2

```

M1 芯片兼容性问题

如果镜像运行失败，检查 Docker Desktop 是否启用了 Rosetta 模拟。

连接失败

确保容器运行正常，检查防火墙（Windows）或网络设置（macOS）是否阻止了端口。

Redis 数据未持久化

确保 docker-compose.yml 中启用了 \--appendonly yes，且 ./redis_data 目录有写权限。



---

## 相关笔记

- [[AI大模型开发总览|AI大模型开发总览]]
- [[1 环境搭建和Milvus向量数据库|环境搭建和Milvus向量数据库]]
