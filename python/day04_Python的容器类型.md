---
tags: [Python]
date: 2026-06-10
---

# day04_Python的容器类型


## 通常指可以包含多个数据，⽀持迭代/索引/切⽚/成员之间关系处理等操作的对象

### 字符串---⽤单引号或双引号定义字符串变量

#### 切⽚是指对操作的对象截取其中⼀部分的操作。字符串、列表、元组都⽀持切⽚操作。

##### (name[7:10:1]

#### 字符串中的查找⽅法

##### find()---没有这个字符返回为-1

##### index()---没有这个字符报错

#### 字符串的修改⽅法

##### replace()

###### 返回替换后的字符串

###### 字符串.replace(要替换的内容, 替换后的内容,替换的次数可以省略)

##### split()

###### 返回切割后的列表序列

###### 返回切割后的列表序列：对字符串进⾏切割操作，返回⼀个list()列表类型的数据

##### title()

###### 所有单词⾸字⺟⼤写

##### join()⽅法

###### 和split()⽅法正好相反，其主要功能是把序列拼接为字符串 

### 列表及其应⽤场景--列表序列名称 = [ 列表中的元素 1,  列表中的元素 2,  列表中的元素 3, ...]用中括号【】

#### 查操作----list1[1]---1是下标

#### 增操作

##### append()

###### 增加指定数据到列表中

##### extend()

###### 列表结尾追加数据，如果数据是⼀个序列，则将这个序列的数据逐⼀添加到列表

##### insert()

###### 指定位置新增数据

#### 删操作

##### del 列表[索引]

###### 删除列表中的某个元素

##### pop()

###### 删除指定下标的数据(默认为最后⼀个)，并返回该数据

##### remove()

###### 移除列表中某个数据的第⼀个匹配项。

#### 改操作

##### 列表[索引]=修改后的值

###### 修改列表中的某个元素

##### reverse()

###### 将数据序列进⾏倒叙排列

##### sort()

###### 对列表序列进⾏排序

#### 列表的循环遍历---循环遍历就是使⽤while或for循环对列表中的每个数据进⾏打印输出

####  列表的嵌套

##### print(end=j+" ")--一行打印

### 元组的定义与使⽤--元组可以存储多个数据且元组内的数据是不能修改的。

#### 定义元组使⽤⼩括号，且使⽤逗号隔开各个数据，数据可以是不同的数据类型。--tuple1 = (10, 20, 30)

#### 元组的相关操作⽅法

### 集合的定义---，我们可以使⽤⼀对花括号{}或者set()⽅法来定义集合，但是如果你定义的集合是⼀个空
集合，则只能使⽤set()⽅法。

#### 集合操作的相关⽅法（增删查）

##### add()⽅法：向集合中增加⼀个元素（单⼀）

##### remove()⽅法：删除集合中的指定数据，如果数据不存在则报错。

##### remove()⽅法：删除集合中的指定数据，如果数据不存在则报错。

##### ①in：判断某个元素是否在集合中，如果在，则返回True，否则返回False

###  Python中字典(dict)的概念

#### 符号为⼤括号（花括号）=> {}

#### ②数据为键值对形式出现 => {key:value}，key：键名，value：值，在同⼀个字典中，key必须是唯
⼀（类似于索引下标）

#### 各个键值对之间⽤逗号隔开

#### user = {'name': 'Tom', 'age': 18}

### 要点

#### 字符串容器，""：数据不可变， 允许重复， 存取有序

#### 列表容器，[]： 数据可变， 允许重复， 存取有序

#### 元组容器，()： 数据不可变，允许重复 ，存取有序

#### 集合容器，{}： 数据可变 ，不允许重复，存取⽆序

## 相关笔记

- [[python/Python笔记总览|Python笔记总览]]


- [[MOC-日常学习]]
