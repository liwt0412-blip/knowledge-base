---
tags:
  - python
date: 2026-06-04
---
# 函数嵌套

### 1 函数可以作为返回值返回函数

### 2  函数可以作为参数互相传递

### 函数名实际上就是一个变量名，都表示一个内存地址

函数名字是地址值 就是一个变量，可以变得，但是（）是函数的方法，变量名变了之后 加（）方法还是一样可以执行

int（）

end=int

end（）此时就跟int（）一样

函数的括号里面可以放函数名字，如果要使用就在函数的内部给函数名加（）

def end（int）：

​	int（）

### global:在局部，引入全局变量，可以在局部修改

### nonlocal：在局部，引入外层的变量，如果有就引入，没有，继续向外一层，直到全局（不包括全局）

# 闭包

### 1：可以让一个变量常驻于内存

### 2. 可以避免全局变量被修改

# 装饰器

### 1.装饰器本质上是一个闭包

### 2.作用：在不改变原有函数调用的情况下，给函数增加功能

### 3.直白：可以在函数前后添加新功能，但是不改变原来的代码

### 4.通用装饰器的写法：

   def   warapper（fn）：					wrappper：装饰器名  fn：目标函数    

​				def inner（*args，**kwargs）：可以调用不同数量的参数

​						#在目标函数执行之前。。。

​						ret=	fn（*args，**kwargs）：可以调用不同数量的参数						#在目标函数执行之后。。。

​						return ret

​				return inner 			千万别加（）

### 5.一个函数可以被多个装饰器装饰

代码先被第一个装饰器装饰后，然后被第二个装饰器装饰

### 6.如果修饰器里面的判断重复使用但是只有第一个需要，可以在去全局增加一个 布尔的变量，在修饰器里面先用global+变量名让我们可以在修饰器面修改这个变量，通过判断这个值是为真还是为假去跳过判断，当第一次执行完成后将布尔值修改成会跳过判断的值。

# 迭代器 

for 变量 in 可迭代的东西

​      prind 变量     -----------------可以打印可迭代的东西里面的每一项

#### 可迭代的东西

str list  tuple dict set open()

可迭代的东西都会提供一个叫迭代器的东西，这个迭代器可以帮我们把数据类型中的所有数据逐一的拿到

#### 获取迭代器的两种方案：

1.iter（）内置函数可以直接拿到迭代器

it = iter（"你叫什么名字啊"）-------------此时it就是迭代器

2. __iter__()特殊方法











#### 从迭代器中拿数据

1.next()内置函数     -----------------括号里面放迭代器 比如上面的it

2.__next __()特殊方法

迭代器是一次性的,数据拿完就不能用了,还需要拿就得再创一个迭代器

### 总结:迭代器统一了不同数据类型得遍历工作

迭代器本身也能被迭代的

迭代器本身的特性:

1只能向前不能反复

2特别省内存

3惰性机制

# 生成器

生成器本质就是迭代器

#### 创建生成器的两种方案

##### 1生成器函数

​	生成器函数中有一个关键字------yield:也有返回的意思,yield只有执行到next的时候才有返回的数据

yield:只要函数中出现了yield,那么这个函数就是一个生成器函数

## 相关笔记

- [[python/Python笔记总览|Python笔记总览]]


- [[MOC-日常学习]]
